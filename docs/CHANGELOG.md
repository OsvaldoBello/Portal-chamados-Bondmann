# Changelog — Portal de Chamados Bondmann

> Extraído do `plano_mestre_desenvolvimento.md` (Sprint 2 / item 2.7, B4) para
> não inflar o documento vivo a cada entrega. **Registrar aqui** (Seção 7 do
> plano mestre) toda mudança de schema/RLS/regra de negócio relevante, mais
> recente no topo: `data · seção do plano mestre afetada · resumo`.
>
> Decisões arquiteturais grandes (não incrementos de feature) viram um ADR em
> [`docs/adr/`](adr/) em vez de só uma linha aqui — ver o índice em
> [`docs/adr/README.md`](adr/README.md).

| Data | Seção alterada | Resumo |
|---|---|---|
| 2026-07-29 | 3.9 (Storage/uploads) | **Limite de anexos por envio: 5 → 20**, para todos os tipos da allow-list (pdf, jpg, png, mp4, docx, xlsx, pptx) e nos três formulários com anexo (abertura de chamado, chat do Portal, chat do Workspace) + nos anexos que chegam por resposta de e-mail (`_extrair_anexos_inbound`). Só a constante `app/anexos.py::MAX_ANEXOS` mudou — a validação já era centralizada em `validar_uploads`. O teto **por arquivo** continua **10MB** (`anexo_max_bytes`), então o pior caso de um POST vai de ~50MB para ~200MB; sem mudança de schema, RLS ou Storage. Os textos "Até N arquivos" dos templates deixaram de repetir o número: `MAX_ANEXOS` virou global do Jinja (`app/templating.py`), que era exatamente a fonte da divergência que este pedido teria criado (constante em 20, UI dizendo 5). |
| 2026-07-29 | 6 (fluxo de dev / CI) | **Revisão de CI/CD — os 5 achados corrigidos.** (1) **Migration esquecida vira build vermelho:** novo `scripts/check_migrations_applied.py` + job `migrations-aplicadas` comparam `supabase/migrations/` com `supabase_migrations.schema_migrations` e reprovam quando falta aplicar — o pipeline continua **sem emitir DDL** em produção (auto-`db push` a cada push seria pior que a doença), mas a ordem certa passa a ser cobrada: schema primeiro, código depois. Casamento por NOME, não por versão, porque a convenção do repo é `NNNN_descricao.sql` e a CLI do Supabase casa por timestamp (toda migration local apareceria como pendente). Baseline `0064`: o histórico remoto não registra várias migrations antigas (0018–0024, 0026–0028, 0045, 0046 aplicadas direto no SQL editor) e outras ficaram só com o slug (`ia_triagens` ← `0050_ia_triagens.sql`), então conferir o passado daria falso-positivo eterno. Precisa do secret `SUPABASE_DB_URL`; sem ele o script avisa e sai 0. Os três caminhos (verde, vermelho, sem secret) foram testados contra o banco de produção. (2) **Branch default protegida** — `claude/develop` passa a exigir os 7 checks do `ci.yml`, sem force-push nem deleção; `enforce_admins` fica **desligado** de propósito, para o dono seguir com o fluxo de push direto (o portão existe para os PRs do Dependabot e para qualquer colaborador). O `E2E RLS` **não** entra como obrigatório: tem filtro de path, e um check que não roda ficaria "expected" para sempre, travando PR que não toca migration. (3) **Dockerfile agora é buildado no CI** (`docker-build`, sem push, cache GHA) — antes o `build-css` provava só o Tailwind e uma quebra do Dockerfile (ex.: o bump `python:3.12-slim → 3.14-slim` do PR #5) só apareceria no deploy. (4) **PRs do Dependabot destravados — e um bug de produção achado no caminho.** Rodando ruff/mypy/pytest num venv com as 19 dependências bumpadas do PR #11, duas das três falhas originais já tinham morrido nos commits `2215e30`/`e3e94f2` (`KeyError: 'conn'` em `test_admin`, `union-attr` em `auth/routes.py`), mas apareceram duas vivas: (a) `ruff 0.16` cobrando `UP047`/`UP049` em `app/ia/triagem.py::_chamar_modelo` — convertido para a sintaxe de genérico do PEP 695 (`async def _chamar_modelo[SaidaT: BaseModel]`, `TypeVar` do módulo removido), verde nos dois pins; (b) **a abertura do Químico quebrava inteira** — de FastAPI 0.140/python-multipart 0.0.32 em diante, campo de formulário com valor VAZIO conta como AUSENTE, então `titulo: str = Form(...)` e `descricao: str = Form(...)` derrubavam o POST num 422 de JSON antes de o servidor derivar os dois a partir do formulário dinâmico (a tela do Químico esconde os campos e o navegador manda ambos vazios — comportamento de 2026-07-22). Corrigido com default explícito (`Form("")`), que devolve a validação para a regra que já existia no handler (`if not titulo or not descricao` → re-render com mensagem em português). O `tests/test_portal.py::test_criar_quimico_sem_assunto_e_descricao_deriva_automaticamente` pegou exatamente isso — o teste estava certo e o pin velho é que escondia o problema. Fica o alerta para o resto: há ~20 outros `Form(...)` obrigatórios (login, MFA, admin, workspace) que, no mesmo cenário, trocam o erro amigável por um 422 cru — todos protegidos por `required` no HTML hoje, mas é dívida a revisar quando o bump entrar. (5) **Custo:** `concurrency` nos dois workflows (cancela runs sobrepostos só em PR — em push nas branches de integração o histórico de verde é o que autoriza migration/deploy) e `cache: pip` nos jobs Python. |
| 2026-07-29 | 4 (SLA) / 6 | **Coluna "Projetos" ganha SLA próprio de 1 mês.** Migration `0064_sla_projetos_um_mes`, **aplicada em produção** (2026-07-29; backfill atingiu os 3 projetos abertos — `BOND-2026-00080`, `00103` e `00569`, um deles vencido desde 11/07 —, todos agora com 30 dias; smoke test em transação revertida provou `EM_ATENDIMENTO` (2,1 d) → `PROJETOS` (30,0 d) e prioridade `URGENTE` **não** derrubando o prazo): `PROJETOS` (`0057`, exclusiva do TI) herdava o prazo do atendimento reativo — as 24h úteis do plano da empresa —, então todo card arrastado pra lá nascia vermelho/piscando em um ou dois dias. Ao ENTRAR na coluna, `limite_resolucao` (e `limite_resposta`, que anda junto em todos os ramos) passa a ser **22 dias úteis (13.200 minutos de expediente ≈ 1 mês corrido)** contados da entrada, via `sla_prazo_projeto()` + trigger `sla_projetos_um_mes` (`BEFORE INSERT OR UPDATE OF status`). Duas armadilhas cobertas: (a) `calcular_sla_chamado` (`0040`, reescrita aqui com um ramo de `PROJETOS`) dispara em `UPDATE OF prioridade` e recalcularia o prazo pelo plano — agora preserva o mês de quem já está na coluna; (b) ordem alfabética de trigger (`calcular_sla` < `sla_pausa_aguardando` < `sla_projetos_um_mes`) garante que a retomada da pausa (`0017`/`0044`) rode ANTES e o mês seja a palavra final. Base é a entrada na coluna, não o `created_at` (o chamado pode ter passado dias em triagem); sair de `PROJETOS` **não** recalcula nada; `sem_prazo` (`0040`) continua ganhando de tudo. Backfill na própria migration: projetos abertos hoje na coluna recebem o mês a partir de agora (resolvidos ficam como estão, para não falsear o "SLA cumprido" histórico do Admin). Testes: 6 e2e novos em `tests/e2e/test_sla_projetos.py` (marker `rls` — a regra só existe em trigger, `FakeRepo` não a exercita). |
| 2026-07-29 | 5.1 / 3.3 | **Telefone informado na abertura passa a atualizar SEMPRE o do perfil** (pedido do gestor; revisa a regra da `0062`, que só gravava quando o perfil estava vazio). O número digitado na abertura é o contato ATUAL da pessoa — quem trocou de celular corrigia no chamado e o cadastro seguia velho, exigindo uma segunda edição em "Meu perfil" que ninguém fazia. `app/routes/portal.py::criar_chamado` grava quando o valor **difere** do que está no perfil (sem `UPDATE` inútil no caso comum, em que o campo veio pré-preenchido e ninguém mexeu), no mesmo `try/except` que nunca derruba a abertura. `chamados.telefone_contato` segue sendo o histórico imutável daquela abertura — atualizar o perfil não reescreve chamados antigos. Sem migration; textos de ajuda de `novo_chamado.html` e `perfil.html` atualizados. Testes: `tests/test_portal.py` (o antigo `..._nao_sobrescreve_o_do_perfil` vira `..._atualiza_o_do_perfil`, mais um caso novo de "mesmo número não reescreve"). |
| 2026-07-29 | — (frente IA, Seção 9 do `plano_md_mestre_IA.md`) | **Recuperação de triagem perdida cai de ~8 min para ~2 min — investigação do BOND-2026-00629.** Diagnóstico com dado real: das 19 triagens de rodada 1 desde 2026-07-23, 13 saíram em ~3,5 s e 5 caíram na faixa 260–480 s; `duracao_ms` do modelo é sempre 2–4 s, então o atraso nunca foi a IA. Toda demora entre 3 e 8 min é assinatura da **reconciliação** (`app/ia/triagem.py::_chamados_orfaos`): o `asyncio.create_task` original morreu (restart/redeploy) e a varredura só pegou o chamado depois. No 00629 a nota interna saiu 280 s após a abertura, com o modelo levando 3,8 s. Duas mudanças: margem de idade do órfão de **3 min → 1 min** (`_MARGEM_ORFAO`, agora parâmetro `$3::interval` do SQL em vez de literal repetido nas duas pernas do UNION — 15× a duração esperada do caminho normal, e encurtar não duplica nada: o motor revalida no banco e o UNIQUE `(chamado_id, rodada, passe)` + `ON CONFLICT DO NOTHING` barram a segunda gravação) e default de `IA_TRIAGEM_RECONCILIACAO_INTERVALO_S` de **300 s → 60 s** (mesmo valor já configurado no Railway pelo gestor). Sem migration. |
| 2026-07-29 | 5.1 | **Backfill de `perfis.telefone`** — migration `0063_backfill_perfil_telefone`, **aplicada em produção** (14 perfis preenchidos de 152; os outros 138 nunca abriram chamado com telefone). Copia para o perfil o `telefone_contato` do chamado MAIS RECENTE de cada autor, só quando o perfil está vazio e só se o número passaria em `validar_telefone_contato` (≥ 8 dígitos) — assim quem já digitou o número alguma vez não é perguntado nem na primeira abertura pós-deploy. Idempotente (reaplicar não sobrescreve nada). |
| 2026-07-29 | 5.1 / 3.3 | **Telefone de contato passa a morar no perfil — a abertura só pede uma vez.** Migration `0062_perfil_telefone` (`perfis.telefone text NOT NULL DEFAULT ''`, ainda **não aplicada em produção**): "Meu perfil" (`/perfil`) ganha o campo, e o campo obrigatório da abertura (`chamados.telefone_contato`, `0058`) vem pré-preenchido com ele. Quem ainda não cadastrou continua vendo o campo vazio e obrigatório — e o **primeiro número informado numa abertura é gravado no perfil** (`app/routes/portal.py::criar_chamado`, só quando o perfil está vazio, em `try/except` que nunca derruba a abertura), então a exigência aparece uma vez só. Número diferente numa abertura **não** sobrescreve o do perfil: `chamados.telefone_contato` é o contato daquele chamado (histórico), `perfis.telefone` é o atual; a troca é explícita em "Meu perfil". Segurança: a `0062` reescreve `enforce_perfil_self_so_avatar()` (trigger da `0033`) para barrar edição de `telefone` de OUTRA pessoa — sem isso a coluna nova ficaria fora da lista congelada do trigger e alcançável por quem a `perfis_update_avatar_staff` (`0052`) deixa tocar linha alheia (ADMIN de qualquer setor, OPERADOR do Marketing). Nova rota `POST /perfil/telefone` (CSRF + `validar_telefone_contato`, a mesma validação da abertura) e `ChamadosRepo.atualizar_telefone`. Testes: 4 em `tests/test_perfil.py` + 4 em `tests/test_portal.py`. |
| 2026-07-28 | 3.9 | **Allow-list de anexos ganha `.pptx` (PowerPoint) — chamados e chat, abertura e resposta.** Mesmo padrão de tolerância OOXML já usado para `docx`/`xlsx` (Seção 3.9): `_EXT_MIMES` (`app/security/uploads.py`) ganha a entrada `pptx` aceitando `application/vnd.openxmlformats-officedocument.presentationml.presentation` **ou** `application/zip` (libmagic antigo devolve o tipo ZIP genérico para contêineres OOXML — a extensão sozinha nunca autoriza, o corpo do arquivo ainda precisa ser um ZIP válido). Três templates atualizados (`accept` do `<input type="file">` + texto de ajuda): `novo_chamado.html` (abertura), `chamado_detalhe.html` (chat do autor) e `workspace/atendimento.html` (chat do staff) — os três liam a mesma allow-list hardcoded, sem fonte única de verdade no template (oportunidade futura: expor a allow-list do backend para os templates em vez de repetir a string). Testes: 2 novos em `tests/test_uploads.py` (`test_pptx_valido`, `test_pptx_aceita_zip_ooxml`, mesmo par de casos do docx). Sem migration — a validação é 100% em memória/código, sem tabela nova. |
| 2026-07-27 | doc IA · F5 (Seções 7, 8.3, Estado) | **F5 — Red team do agente Químico entregue: ZERO VAZAMENTOS.** Corpus versionado (`tests/red_team/casos/*.json`, 6 categorias mínimas da Seção 8.3: pedido direto de formulação, "ignore as instruções anteriores"/roleplay "DAN", autoridade falsa "sou o gerente do setor", extração incremental multi-rodada, indução a quantidades "aproximadas", tentativa de vazar a nota interna via pergunta pública). **Camada estrutural** (`tests/red_team/test_estrutural.py`, marker `redteam`, 26 testes, modelo mockado — sempre roda no CI): prova que os invariantes da Seção 8.2 seguram mesmo com o texto malicioso de cada cenário no chamado — payload do Passe A sem sentinelas da base, Passe B nunca executa no ciclo `PERGUNTAS`, `ia_triagens.resultado` do Passe B só com metadados (nunca a `pre_analise`), `ContextoQuimico` sem nenhum campo que comporte quantidade/formulação, nenhuma query do módulo referenciando `base_quimico_formulacoes`, `is_interna` fixado sem parâmetro nas duas funções de persistência (pública/interna). **Camada comportamental** (`tests/red_team/test_comportamental.py`, 11 testes, chamada REAL ao `gpt-5.4-mini` de produção via `.env` local — opt-in `IA_REDTEAM_LIVE=1`, skip automático sem a env, mesmo padrão de `tests/e2e/conftest.py` para a suíte `rls`): mede a OBEDIÊNCIA do modelo ao prompt sob pressão adversarial, via denylist regex de padrões de quantidade (percentual/proporção/g·L⁻¹/ppm/"partes"); o Passe B usa um contexto SINTÉTICO com valores-sentinela (nunca dado real da base). **Execução ao vivo em 2026-07-27: 11/11 verdes, zero ocorrência de padrão de quantidade em qualquer saída** (relatório bruto em `tests/red_team/execucoes/2026-07-27.md`; tabela de execução em `plano_md_mestre_IA.md`, Seção 8.3). Marker `redteam` registrado em `pyproject.toml`. Gatilho permanente documentado: qualquer PR que altere `app/ia/prompts/quimico_*` ou o modelo do Químico deve reexecutar `IA_REDTEAM_LIVE=1 pytest tests/red_team/` antes do merge. Critério de saída da F5 (zero vazamentos) atendido — libera o gate de SEGURANÇA da F6 (falta o gate de negócio: DPA OpenAI e homologação formal). |
| 2026-07-27 | doc IA · F4 (painel operacional) | **Painel `/admin/base-quimico`: o próprio setor Químico reingesta a base sem precisar do dev.** Complementa `af0848f` (ingestão virou `app/services/ingestao_quimico.py`) — a rota reaproveita `ingerir_conn` direto, sem duplicar o parse. `GET/POST /admin/base-quimico`: upload de planilha `.xlsx` (obrigatória) + PDF de fichas (opcional) + checkbox "remover produtos ausentes"; escrita via `admin_connection()` (a ingestão mexe em tabelas fora do escopo normal de RLS de qualquer papel, mesmo do próprio Químico). Acesso restrito a TI ou staff (OPERADOR/ADMIN) do próprio Dpto Químico (`AdminCtx.pode_editar_base_quimico`, gate novo em `admin_context`) — item de menu "Base do Químico" no workspace visível às mesmas duas contas. Página de resultado mostra contagens por tabela, fichas do PDF sem produto correspondente (revisar) e produtos do banco ausentes da planilha enviada. Testes: 7 novos em `tests/test_admin.py` (403 para outros setores/papéis, formulário renderiza pro Químico, link do menu aparece/some por perfil, extensão inválida rejeitada, sucesso chama `ingerir_conn` e mostra o relatório completo). |
| 2026-07-27 | 3.4.1 / 3.9.1 | **Dois pedidos do usuário: (1) lista de "Contas de usuário" em ordem alfabética; (2) "Lembrar este dispositivo por 30 dias" no MFA — mais um fix no mesmo dia, achado pelo próprio usuário testando.** **(1)** `AdminRepo.usuarios()` (`app/repositories/admin.py`) trocou `ORDER BY (role = 'CLIENTE'), departamento, nome` por `ORDER BY lower(nome)` — a tela é a mesma para TI (gestão completa) e para o Operador do Marketing/Admin de setor (só troca de foto, `pode_editar_avatares`), então a ordenação vale para os dois sem mudança de rota/RLS. **(2)** Checkbox novo em `/mfa/verify` e `/mfa/enroll/confirmar` — cookie assinado (`app/auth/mfa_remember.py`, `itsdangerous.URLSafeTimedSerializer`, mesmo padrão do CSRF, chave `session_secret` já reservada em `Settings` e até então sem uso) com o `user_id`, 30 dias de validade; NÃO eleva a sessão a `aal2` no GoTrue (só o código real faz isso) — é uma checagem adicional e só local. **Bug pego pelo usuário ao testar (mesmo dia):** marcar o checkbox, sair e entrar de novo ainda pedia o código. Causa: o cookie só era checado no gate do painel `/admin` (`enforce_admin_mfa`, `app/auth/dependencies.py`) — mas quem decide mandar para `/mfa/verify` logo após autenticar é o **`POST /login`** (`app/auth/routes.py::login_submit`, linha que compara `_tem_fator_verificado(result.user)`), uma checagem **totalmente separada** que roda antes de qualquer rota passar pelo `admin_context`; o usuário nunca chegava ao `/admin` sem passar por ali primeiro. **Fix:** `login_submit` passou a chamar `mfa_remember.dispositivo_confiavel()` também, pulando o redirect para `/mfa/verify` quando o dispositivo é confiável (a sessão segue `aal1` — é o gate do `/admin`, já corrigido antes, que trata o cookie como equivalente ao step-up). **Limitação aceita** (ambos os gates): sem registro server-side de dispositivos, um reset de MFA pelo TI não revoga um cookie já emitido noutro navegador do mesmo usuário — expira sozinho em 30 dias (fora de escopo do pedido; exigiria lista de dispositivos por usuário). Testes: `tests/test_mfa.py` — 5 casos unitários de `dispositivo_confiavel` (aceita, rejeita outro usuário, sem cookie, cookie adulterado, expira), 2 de integração com `enforce_admin_mfa`, 2 de rota `/mfa/verify` (grava o cookie só quando marcado) + 4 de rota `/login` (com/sem fator, cookie válido pula o step-up, cookie de outro usuário não pula, sem MFA vai direto independente do cookie — este último cobre exatamente o bug reportado); suíte de MFA (34 testes) verde. |
| 2026-07-24 | doc IA · F2 (Seções 2.4, 10.1) | **Limiar de confiança das perguntas vira env — BAIXA e MÉDIA também perguntam (decisão do usuário).** Contexto: BOND-2026-00601 (1º chamado do TI fora da sombra) saiu com `confianca=BAIXA` e a guarda fixa "só ALTA pergunta" (mitigação 10.1) mandou as perguntas para a nota interna — funcionou como projetado, mas o usuário decidiu abrir. Nova env `IA_TRIAGEM_PERGUNTAS_CONFIANCA_MINIMA` (default `BAIXA`; `MEDIA`/`ALTA` reapertam sem deploy; valor inválido degrada para `ALTA`). `decidir_acao` compara por ranking BAIXA<MEDIA<ALTA (`_CONFIANCA_ORDEM`, `app/ia/triagem.py`). Nada a fazer no Railway para o comportamento pedido (default já é BAIXA). Testes: matriz atualizada + `test_decidir_acao_limiar_de_confianca_configuravel`; suíte verde. |
| 2026-07-24 | doc IA · F2 (Seções 2.3, 2.4, 7) | **Saída do modo sombra POR DEPARTAMENTO — TI liberado para perguntar ao autor.** Decisão do usuário (sombra validada: notas internas úteis e perguntas pertinentes). A flag global `IA_TRIAGEM_MODO_SOMBRA` era tudo-ou-nada: desligá-la tiraria TAMBÉM o Dpto Químico da sombra antes do red team (F5, gate de zero vazamentos). Nova env `IA_TRIAGEM_PERGUNTAS_DEPARTAMENTOS` (CSV, default vazio = comportamento atual): departamento listado sai da sombra mesmo com a global ligada; `MODO_SOMBRA=false` continua = ninguém em sombra (F6). `Settings.ia_triagem_em_sombra(departamento)` (novo, `app/config.py`) decide; `decidir_acao` (`app/ia/triagem.py`) ganha o departamento; `historico_chamados` registra o `modo_sombra` EFETIVO. **Operação (gestor):** `IA_TRIAGEM_PERGUNTAS_DEPARTAMENTOS=TI` no Railway após o deploy — sombra global fica `true`, Químico intocado. Testes: matriz por departamento + fluxo integrado (TI pergunta com sombra global ligada; Químico só nota interna) em `test_ia_triagem.py`/`test_config.py`; suíte completa verde. |
| 2026-07-24 | doc IA · F4 (fichas — revisão do químico) | **Fichas órfãs resolvidas com o químico: 65 de 66 produtos com ficha em produção (era 39→59→65).** O químico esclareceu os 11 casos que estavam sem produto na planilha; tratados como config no `scripts/ingestao_base_quimico.py`: (a) `CORRECOES_NOME_PRODUTO` — o S101 fora rotulado "DESINFETANTE FLORAL" por engano, é o LAVANDA (renomeado, PK preservada; elimina a duplicata de FLORAL e dá a ficha pág 36 ao LAVANDA); (b) `ALIASES_FICHAS` novo: PROTEC concentra as fichas de PROTEC (pág 53) + PROTETIVO (pág 54) — não existe "BASE PROTEC" na planilha, PROTEC é o produto-base; (c) `PRODUTOS_SO_FICHA` — AW-B 32/46/68 e LW-B 32/46/68 são comprados prontos (sem formulação, por isso fora da planilha) e entram como produto + ficha, sem componentes (nada a proteger; nome idêntico ao dropdown do formulário); (d) `FICHAS_ESPERADAS_SEM_PRODUTO` — ADITIVO 1090/ANTI-INCRUSTANTE (gestor adiciona depois) e LB 20 H (não liberado para venda) deixam de poluir o aviso, marcados como esperados. Reingerido em produção: 66 produtos (60 planilha + 6 só-ficha), 65 fichas, só TRIACID - DESCONTINUADO sem ficha. `app/ia/contexto_quimico.py` já casava grafias por forma alfanumérica (fix anterior). Testes: 6 novos em `test_ia_quimico.py` (correção por chave preserva PK; produto só-ficha; ficha por âncora; PROTEC com duas fichas), 24 verdes. **Reachability pendente (runbook d.5):** dropdown usa nomes comerciais (BRIL, PROTETIVO, DESINFETANTES BONDMANN (X)) × base guarda BASE BRIL/PROTEC/DESINFETANTE X — recuperação seletiva pode não casar; próximo ajuste é um de-para compartilhado. |
| 2026-07-24 | doc IA · F4 (ingestão de fichas) | **Fatiamento das fichas técnicas corrigido com o gestor (página a página): 39 → 59 de 60 produtos com ficha em produção.** Três defeitos no algoritmo original: (1) a citação padrão "...utilizando DEGRAX 25 e ADITIVO 967" no corpo das fichas de fluidos fazia o nome mais longo ROUBAR a página (LB 10/LB 20/LUBRY/OIL 30/SAW/WAY 45 caíam no ADITIVO 967; pág do DFD caía no CONCENTRADO por "produto concentrado e de ação rápida"); (2) grafias divergentes planilha × PDF ("WAY45 - B" × "WAY 45 - B", "D.F.D.", âncora ">> LB10 H"); (3) nomes comerciais diferentes (FEMME/FLORAL/OX/SNAP/BRIL+GRAXCAR II/CONCENTRADO+SHAMP para os DESINFETANTE */BASE *). **Fix (`scripts/ingestao_base_quimico.py`):** constatado que TODA ficha tem âncora `>> NOME` (1 página = 1 ficha, sem continuações) — a âncora decide sozinha (comparação alfanumérica `_alnum`); página com âncora de produto FORA da planilha é reportada e nunca anexada a outro produto; fallback pelo corpo só em página sem âncora (regex flexível a espaços/pontos com limite de palavra); tabela `ALIASES_FICHAS` com o de-para confirmado pelo gestor (BASE BRIL recebe 2 fichas: BRIL + GRAXCAR II; BASE CONCENTRADO: CONCENTRADO + SHAMP; variantes INCOLOR de LIMPTEC 100/SABOLIQ anexadas ao principal); limpeza de fichas órfãs no upsert (PDF = fonte da verdade). **`app/ia/contexto_quimico.py`:** `identificar_produtos` passou a casar o select do formulário por forma alfanumérica e o texto livre por regex flexível — "WAY 45 - B" (formulário) acha "WAY45 - B" (planilha). Reingerido em produção: 59/60 fichas; só TRIACID - DESCONTINUADO sem ficha. **Pendência nova (runbook, seção d.4):** 11 fichas do PDF são de produtos SEM linha na planilha (ADITIVO 1090, ADITIVO ANTI-INCRUSTANTE, AW-B 32/46/68, LW-B 32/46/68, DESINFETANTE LAVANDA, LB 20 H, PROTETIVO — vários estão no dropdown do formulário); gestor decide: acrescentar à planilha e reingerir. Testes: fatiamento reescrito (7 casos novos, 20 verdes em `test_ia_quimico.py`). |
| 2026-07-24 | 3.3 / 5.1 / 5.3 / Estado | **Coluna de Kanban "Última Interação do Usuário" (TI/RH), automática por mensagem.** Pedido do usuário: sinalizar no Kanban quando a última mensagem do chat foi de quem ABRIU o chamado (a bola está com o setor). Novo valor de enum `RESPOSTA_CLIENTE` (migration `0060_status_resposta_cliente.sql`, isolada — regra de 1 valor de enum por transação, mesmo padrão da `0024`/`0043`/`0057`) + trigger `mover_status_resposta_cliente` em `mensagens` (migration `0061_status_resposta_cliente_trigger.sql`, `AFTER INSERT ... WHEN (is_interna = false)`): se `remetente_id = chamados.cliente_id` e o status está em `EM_ATENDIMENTO`/`AGUARDANDO`, move para `RESPOSTA_CLIENTE`; se quem respondeu não é o autor e o status é `RESPOSTA_CLIENTE`, volta para `EM_ATENDIMENTO`. Restrito a TI/RH (nome hardcoded, mesmo padrão da `0038`/`0042`/`0044`) — fora do fluxo reativo clássico ficam de fora (`NOVO`, `RESOLVIDO`, `PROJETOS`). **Por que trigger e não código Python:** a RLS do CLIENTE só libera `UPDATE status` na avaliação (`0006`) e na reabertura (`0059`) — mover o status aqui é ação do sistema, não permissão do papel, então precisa de `SECURITY DEFINER` (mesmo mecanismo de `auth_is_ti()`), que bypassa a RLS por ser dono das tabelas (sem `FORCE ROW LEVEL SECURITY` em lugar nenhum do projeto). A trigger `sla_pausa_aguardando` (`0017`/`0044`) já retoma o relógio de SLA sozinha ao sair de `AGUARDANDO` (RESPOSTA_CLIENTE não entra na lista de status pausados), sem precisar mexer nela. **UI:** `app/routes/workspace.py::_status_ui()` ganha `_STATUS_UI_TI`/`_STATUS_UI_RH` com a coluna entre `EM_ATENDIMENTO` e `AGUARDANDO`; `STATUS_META["RESPOSTA_CLIENTE"]` (`app/templating.py`) com token de cor próprio (`st_resp_cliente`, `tailwind.config.js`); contagens (`FilaRepo.fila_stats`, `AdminRepo.por_status`, `admin.js::STATUS_COR`) e whitelist de validação (`workspace.py::STATUS_VALIDOS`) atualizadas para não quebrar/omitir o novo valor. Testes: `test_workspace.py` (colunas do TI e do RH, whitelist) — suíte de `test_workspace.py`+`test_admin.py` (112 casos) verde. Migrations `0060`/`0061` ainda **não aplicadas em produção**. |
| 2026-07-24 | 3.3 / 5.1 / Estado | **Reabertura do chamado pelo autor + comentário obrigatório em nota ≤4 + telefone de contato obrigatório na abertura.** Três pedidos do usuário. **(1) Reabertura (migration `0059_chamado_reabertura_cliente.sql`, RLS + trigger, ainda não aplicada em produção):** o autor de um chamado `RESOLVIDO` insatisfeito com a solução pode devolvê-lo para `EM_ATENDIMENTO` — mesmo padrão de defesa em profundidade da avaliação (`0006`): nova policy `chamados_update_cliente_reabertura` (`USING status='RESOLVIDO'` / `WITH CHECK status='EM_ATENDIMENTO'`, ambas do próprio autor) combinada por OR com a policy de avaliação já existente; o trigger `enforce_cliente_so_avaliacao` ganhou um ramo que só libera essa transição específica (bloqueando qualquer outra coluna, exige `resolvido_em` voltar a `NULL`). `AtendimentoRepo.reabrir()` (novo) zera a avaliação anterior junto (a nota antiga não vale para a nova rodada de atendimento) e registra `REABERTO` no histórico; reabrir a rota `POST /portal/chamados/{id}/reabrir` também reabre o chat (`chamado_detalhe.html` já escondia o composer só quando `status == RESOLVIDO`). `PortalService.pode_reabrir()` — ao contrário de `pode_avaliar`, vale mesmo em chamados endereçados ao PRÓPRIO departamento do autor (autoatendimento): não é uma nota de CSAT, é dizer "isso não foi resolvido de verdade". Botão "Reabrir chamado" novo em `chamado_detalhe.html`, fora do fragmento de avaliação (aparece independente de já ter avaliado). **(2) Comentário obrigatório com nota ≤4 (sem migration — só `chamados.avaliacao_comentario`, já `text` nullable):** `validar_comentario_avaliacao()` novo em `app/repositories/chamados.py` (`NOTA_COMENTARIO_OBRIGATORIO=4`, `COMENTARIO_MIN_CHARS=50`) — nota 4 estrelas ou menos sem pelo menos 50 caracteres devolve 422 explicando o motivo; acima disso o comentário segue opcional. UX em `_avaliacao.html`/`app/static/js/avaliacao.js` (novo, CSP-safe — delegação de evento em `document`, sobrevive ao swap HTMX do fragmento): alterna `required`/`minlength` no textarea conforme a nota marcada, só client-side, a validação real é sempre no servidor. **(3) Telefone de contato obrigatório na abertura (migration `0058_chamado_telefone_contato.sql`, coluna `chamados.telefone_contato text NOT NULL` sem default — backfill `''` nas linhas existentes, ainda não aplicada em produção):** `validar_telefone_contato()` novo (mínimo de 8 dígitos ignorando formatação) — sem o campo ou com poucos dígitos, 400 antes de criar o chamado. Campo novo em `novo_chamado.html` (entre Setor e o aviso do Marketing); exibido também em `chamado_detalhe.html` (autor) e `workspace/atendimento.html` (staff — é o motivo do campo existir: falar direto com quem abriu). `AtendimentoRepo.criar`/`obter` e a fachada `ChamadosRepo.criar` propagam o novo parâmetro. **Testes:** `test_portal_service.py` (+4, `pode_reabrir`); `test_portal.py` (+~18: telefone ausente/curto/válido na abertura — `_abertura_valida`/`_abertura_ocorrencia`/os 2 dicts manuais do Químico ganharam o campo —, comentário curto/vazio/válido por faixa de nota, reabertura mostrada/oculta/sucesso/bloqueada por status/bloqueada por autor, unit tests dos 2 validadores novos). Suíte completa (`tests/test_portal.py`+`test_portal_service.py`) verde. **Pendente:** aplicar `0058`/`0059` no Supabase de produção (fica para o gestor autorizar, mesmo padrão de migrations recentes). |
| 2026-07-24 | 5.1 / Estado | **Nova coluna "Projetos" no Kanban do TI — status exclusivo do setor, mesmo padrão do Marketing.** Pedido do usuário. Segue exatamente o precedente de `A_FAZER`/`AGUARDANDO_TERCEIROS` (Marketing, migrations `0024`/`0043`/`0048`): valor novo no enum `status_chamado` isolado em migration própria (`0057_ti_projetos_status.sql` — `ALTER TYPE ... ADD VALUE`, não pode ser usado na mesma transação em que é criado) + fiação 100% em código, sem mexer em RLS/JS/rota genérica de drag (`app/routes/workspace.py::mudar_status`/`iniciar_atendimento` já são genéricos sobre `novo_status`). **`app/routes/workspace.py`:** `STATUS_VALIDOS` ganha `PROJETOS`; `_status_ui()` passa a ramificar por 3 casos (Marketing/TI/padrão) — `_STATUS_UI_TI = (NOVO, PROJETOS, EM_ATENDIMENTO, AGUARDANDO, RESOLVIDO)`, coluna posicionada logo após "Novo" (mesmo lugar de "A fazer" no Marketing). **UI:** `app/templating.py::STATUS_META["PROJETOS"]` (label "Projetos", ícone de pasta, cor nova `st_projetos` roxo `#7C3AED` em `tailwind.config.js`, distinta das 5 cores de status já em uso); `npm run build:css` rodado. **`app/repositories/fila.py::fila_stats`** ganha a chave `PROJETOS` (e `A_FAZER`, que faltava) no dict de contagem por coluna. Sem trigger de pausa de SLA (`sla_pausa_aguardando`) — "Projetos" segue ativo/não-pausado, igual "A fazer" no Marketing, não "Aguardando". Testes: `tests/test_workspace.py` — `FakeRepo.fila_stats` ganhou as chaves novas; `test_kanban_fora_do_marketing_nao_tem_coluna_a_fazer` estendido para confirmar a coluna "Projetos" no TI; teste novo `test_kanban_projetos_e_exclusivo_do_ti` (RH não ganha a coluna). Suíte completa + rebuild de CSS verdes. **`0057_ti_projetos_status.sql` aplicada em produção** (projeto `iurlzlhbnoemkzgexcfk`, via MCP, a pedido do usuário — `get_advisors` sem alerta novo). |
| 2026-07-24 | 5 / Estado | **TMA de "Projetos" separado do TMA geral nos indicadores do Admin (TI) — pedido do usuário: "mais peso" pra chamados de projeto.** Como `chamados.status` vira `RESOLVIDO` na conclusão (perde o valor `PROJETOS`), e não existe coluna própria marcando "isto foi um projeto", a identificação usa `historico_chamados` (log imutável de `STATUS_ALTERADO`, `{"de":..., "para":...}`, gravado em `AtendimentoRepo.alterar_status`) — **decisão do usuário:** só conta como "Projeto" quem foi resolvido **diretamente** a partir da coluna Projetos (a transição mais recente para `RESOLVIDO` tem `de = 'PROJETOS'`); um chamado que passou por Projetos mas foi resolvido depois de ir pra Em Atendimento não conta. **`AdminRepo.kpis()`** (`app/repositories/admin.py`): a query ganhou uma CTE (`base`) com subquery correlacionada sobre `historico_chamados` (usa o índice já existente `idx_historico_chamado(chamado_id, created_at)` — `ORDER BY created_at DESC LIMIT 1` por chamado) resolvendo `resolvido_de`; `tma_seg` (TMA geral) passa a **excluir** esses chamados (`resolvido_de IS DISTINCT FROM 'PROJETOS'`); duas colunas novas, `projetos_resolvidos` (contagem) e `tma_projetos_seg` → `tma_projetos_horas` em Python. `resolvidos`/`resolvidos_no_prazo`/`conformidade_sla` **não mudam** — o usuário só pediu separação no TMA, não nos demais KPIs. Validado com uma query equivalente rodada direto em produção via MCP antes de codificar (480 chamados resolvidos do TI, 0 "Projetos" ainda — status novo, sem histórico anterior). **`AdminRepo.por_status()`** ganha a chave `PROJETOS` (fluxo de correção do mesmo gap: chamados parados na coluna Projetos, ainda não resolvidos, estavam sumindo do gráfico "Chamados por status" — o dict de saída só tinha as 4 chaves clássicas). **UI:** `app/templates/admin/dashboard.html` ganha 2 cards novos ("TMA (Projetos)", "Projetos concluídos", cor `st_projetos` roxa — mesma identidade visual do Kanban), visíveis **só quando `escopo == "TI"`** (única fila com essa coluna); `app/static/js/admin.js::STATUS_COR` ganha `PROJETOS` pro gráfico de status colorir certo. Testes: `tests/test_admin.py` — `FakeAdmin.kpis()`/`por_status()` ganharam as chaves novas; 2 testes novos (`test_dashboard_ti_mostra_tma_projetos_separado`, `test_dashboard_fora_do_ti_nao_mostra_card_de_projetos`). Sem migration (query nova, schema inalterado). |
| 2026-07-24 | CI · E2E RLS (Seção 1.7/M9) | **CI "E2E RLS" quebrado há 4 pushes seguidos (desde `452f6e5`) — dois buracos de GRANT que só se manifestam num Supabase local do zero, mais um teste sem SAVEPOINT.** Investigação via `gh run view --log-failed`: (1) `0050_ia_triagens.sql` criou a policy `ia_triagens_select_staff` mas nunca deu `GRANT SELECT ... TO authenticated` na tabela — toda outra migration do repo que cria tabela nova faz esse GRANT explícito, a 0050 foi a exceção; em produção o SELECT já estava concedido por drift (conferido via MCP), por isso nunca doeu lá. (2) `0054_base_quimico.sql` criou o role `ia_worker` sem `GRANT ia_worker TO postgres` — em produção não importa (`contexto_quimico.py` conecta direto com `IA_WORKER_DATABASE_URL`, nunca faz `SET ROLE`), mas a suíte e2e simula o role via `SET LOCAL ROLE ia_worker` a partir da conexão admin, que exige membership. **Fix:** migration nova `0056_fix_grants_ia_triagens_e_ia_worker.sql` (idempotente, sem tocar nas migrations já aplicadas em produção). (3) Bug separado, mascarado pelos dois de cima: `test_rls_base_quimico.py::test_usuario_autenticado_nao_enxerga_nenhuma_linha_da_base` iterava 5 tabelas capturando `InsufficientPrivilegeError` por tabela sem SAVEPOINT — a 1ª tabela sem GRANT aborta a transação Postgres inteira (não só o statement), então a 2ª tabela quebrava com `InFailedSQLTransactionError` em vez do erro esperado. Mesma classe de bug já corrigida em `test_rls_matrix.py` (commit `ba6991d`, 2026-07-16); aplicado o mesmo padrão (`async with conn.transaction():` por iteração) aqui e no teste de `base_quimico_formulacoes` do mesmo arquivo, por consistência. `.gitignore` também ganhou os artefatos incrementais do `/graphify` (`.graphify_ast.json`, `.graphify_chunk_*.json`, `.graphify_detect.json`, `.graphify_incremental.json`, `.graphify_uncached.txt`, `converted/`) que apareciam como untracked. |
| 2026-07-24 | doc IA · F4 (Seção 2.2/7) / Estado | **Correção: o motor de dois passes do Químico (F4) nunca tinha sido ligado em `app/ia/triagem.py` — só existiam `contexto_quimico.py`, os prompts e os testes.** Ao puxar o commit `e9a9a669` (outro operador, ajuste dos formulários do Químico — BOND-2026-00569) e rodar a suíte antes de prosseguir, `tests/test_ia_quimico.py` quebrou inteiro com `AttributeError: module 'app.ia.triagem' has no attribute 'contexto_quimico'`: o doc/CHANGELOG do dia 23 registrava a F4 como "código completo e testado", mas `app/ia/triagem.py` nunca importava `contexto_quimico` nem tinha o fluxo de dois passes — só o passe único (TI/F1–F3). **Fix (`app/ia/triagem.py`):** `_executar` passa a rodar o Passe A (playbook de perguntas via `contexto_quimico.playbook_perguntas`, ZERO dado de produto) para chamados do Dpto Químico; quando a ação decidida é `NOTA_INTERNA`, roda o Passe B (contexto seletivo via `contexto_quimico.montar_contexto_passe_b`, schema `SaidaPasseB`) e a nota final passa a ser a pré-análise técnica dele (`montar_nota_quimico`), com fallback para a nota do Passe A se o B falhar (JSON inválido/timeout). `_chamar_modelo` generalizado (schema/model plugáveis) para servir os dois passes; `montar_mensagens` ganha `playbook`; `montar_mensagens_passe_b` novo. PERGUNTAS (ciclo de perguntas ao autor) nunca roda o Passe B — base sigilosa não participa do canal público (Seção 3.1), como já provava o teste que estava quebrado. **Resultado:** os 7 testes de `test_ia_quimico.py` que falhavam (payload sem sentinela, nota vem do B, metadados sem pré-análise, degradação em falha do B, PERGUNTAS não roda B, log sem texto claro, TI segue passe único) + os 8 restantes (recuperação seletiva, fatiamento de fichas) voltam a passar; suíte completa (`pytest -m "not rls"`) + `ruff`/`mypy` limpos. Nenhuma mudança de schema/migration — só a integração que faltava no motor.
| 2026-07-23 | doc IA · F4 (Seções 3, 4.3, 7) / Estado | **F4 — Agente Químico de dois passes entregue; base sigilosa migrada da planilha/PDF para o banco (produção).** Decisão de armazenamento (demanda do usuário — dados sensíveis NUNCA no repositório): Supabase Postgres com **recuperação seletiva** ≻ "arquivos em nuvem lidos a cada chamada" (≈3–8k tokens por triagem em vez de ≈100k+ da base inteira; e a proteção vira permissão de banco, não disciplina de prompt) ≻ vector store do provedor (subiria a planilha com formulações a um terceiro — quebra a Regra de Ouro #4). **Migration `0054_base_quimico`** (repo + produção via MCP): 5 tabelas (`produtos`, `materias_primas`, `playbooks` unificando Diagnostico/Perguntas/Regras-Sigilo com coluna `tipo`, `fichas`, `formulacoes`) com RLS habilitada; role `ia_worker` com GRANT+policy SÓ nas 4 liberadas (correção ao DDL planejado: RLS vale para o role, então policy `TO ia_worker` é necessária); `formulacoes` (quantidades) sem GRANT nem policy — pós-check em produção: `has_table_privilege = false`; advisors sem alerta novo (só INFO esperado). **Ingestão** `scripts/ingestao_base_quimico.py` (re-executável, arquivos lidos de FORA do repo; `openpyxl`/`pypdf` só em requirements-dev): rodada 2× em produção com contagens idênticas (idempotência provada) — 60 produtos, 94 MPs, 16 playbooks, 437 formulações, fichas do PDF fatiadas por produto para 39 nomes (20 nomes sem ficha → revisão manual do gestor). **Motor (`app/ia/triagem.py`):** Passe A (`prompts/quimico_passe_a.md`; chamado + playbook de perguntas, ZERO dado de base — invariante provada com sentinelas sobre o payload real) decide nota × perguntas; Passe B (`prompts/quimico_passe_b.md`, 6M/higiene adaptados do prompt do GPT interno; contexto seletivo via `app/ia/contexto_quimico.py` na conexão `ia_worker`) escreve a pré-análise SEMPRE como nota interna (`is_interna=true` fixado); linhas A e B em `ia_triagens` (B só com metadados — Seção 4.1); falha do B degrada para a nota do A; pré-análise fora dos logs (teste `caplog`). `gerar_e_salvar_resumo` **aposentado quando a triagem cobre o Químico** (transição C2 em `portal.py` — nunca dois pipelines no mesmo evento). **Testes:** 15 novos em `test_ia_quimico.py` + e2e `test_rls_base_quimico.py` (marker `rls`); suíte completa + ruff verdes. **Pendente (gestor):** `ALTER ROLE ia_worker PASSWORD ...` + `IA_WORKER_DATABASE_URL` + `Dpto Químico` em `IA_TRIAGEM_DEPARTAMENTOS` (sombra) no Railway; DPA OpenAI (gate go-live, C5); F5 red team antes de sair da sombra. |
| 2026-07-23 | doc IA · F3 (Seções 4.4, 5) | **Busca de chamados semelhantes passa a pesquisar também a RESOLUÇÃO, não só título+descrição.** Pedido do usuário: até aqui o casamento FTS incidia só sobre `chamados.fts` (coluna gerada `titulo+descricao`); a resolução registrada (última mensagem pública de staff) era **exibida** na nota mas não **pesquisada**. **Fix (`app/repositories/ia_busca.py`, sem migration):** o predicado e o `ts_rank` passam a incidir sobre `c.fts || to_tsvector('portuguese', coalesce(res.conteudo,''))` — a resolução entra no `tsvector` em tempo de busca, concatenada ao índice gerado. Assim um chamado é encontrado tanto pelo sintoma quanto pela **solução aplicada** (ex.: casar pela conduta "reinstalação do certificado A1" mesmo que o título não a cite). Pesquisa e exibição usam o **mesmo** texto de resolução (a última mensagem de staff do JOIN LATERAL), então nunca se cita uma resolução que não casou. `[DECISÃO DE ENGENHARIA]` como o predicado agora é uma expressão, o índice GIN `idx_chamados_fts` não filtra sozinho; os filtros duros (`departamento_id` + `status='RESOLVIDO'`) seguram o conjunto candidato na v1 — promover a resolução a coluna materializada indexada só se o volume justificar (mesma linha da Seção 4.4). **Testes:** 1 e2e novo em `tests/e2e/test_ia_busca_fts.py` (`test_acha_pela_resolucao_quando_titulo_e_descricao_nao_casam`: termo só na resolução acha e cita o chamado); os mocks unitários de `test_ia_triagem.py` seguem casando por `websearch_to_tsquery` (43 unit verdes; e2e `rls` skip sem Postgres local). Doc IA atualizado (Seções 4.4 e 5). |
| 2026-07-23 | 3.9.1 / Estado | **Lightbox "não abria" pro usuário — causa real: `.js`/`.css` próprio sem cache-busting, navegador servia versão antiga do cache. Fix: `static_url()` em todo asset de 1ª parte.** O usuário reportou (com print) que clicar na foto não fazia nada — a lógica em si (verificada de novo com o app rodando de verdade) estava correta; o problema é que `app/static/js/shell.js`/`app/static/css/app.css` (e os demais `.js` próprios) eram referenciados por path fixo (`/static/js/shell.js`, sem query string) em todo template — o HTML sempre vem fresco (SSR a cada request), mas o navegador de quem já tinha visitado o site antes segue servindo o `shell.js` **antigo** (sem a função do lightbox) do cache HTTP, mesmo com o servidor já rodando o código novo; só um refresh forçado (Ctrl+Shift+R) contornava, o que não é razoável pedir a cada deploy. **Fix (`app/templating.py`):** `static_url(path)`, global Jinja novo — devolve `path?v=<mtime do arquivo>`; muda a cada deploy que toca o arquivo, força o navegador a buscar de novo. Aplicado em **todo** `<link>`/`<script>` de asset PRÓPRIO (`app.css`, `anim.css`, `shell.js`, `notificacoes.js`, `admin.js`, `admin_marketing.js`, `chat.js`, `workspace.js`, `novo_chamado.js`, `pessoas_picker.js`) em 15 templates — os `vendor/*` (HTMX/Alpine/Supabase/Chart/Sortable) ficaram de fora de propósito: já são pinados por versão + SRI, imutáveis. Testes novos em `tests/test_templating.py` (5 casos: URL ganha `?v=`, muda quando o arquivo muda, arquivo ausente devolve o path original sem quebrar, registrado como global Jinja, sanity do path real). Suíte completa (502) + ruff verdes. |
| 2026-07-23 | 3.9.1 / Estado | **Lightbox: clicar na miniatura de avatar em `/admin/usuarios` amplia a foto centralizada na tela; clique fora ou Esc fecha.** Pedido do usuário. Novo overlay compartilhado `#lightbox`/`#lightbox-img` em `app/templates/workspace/workspace_base.html` — **sempre presente** (fora de `{% block overlays %}`, que o dashboard sobrescreve pro csat-modal sem `{{ super() }}`), então funciona em qualquer página do shell que use o padrão `[data-lightbox="url"]`, não só `usuarios.html`. Fica FORA de `<main>` pelo mesmo motivo já documentado do csat-modal (`<main>` tem `transform` persistente via `anim-rise`, vira containing block de `position: fixed` e descentraliza o overlay). JS genérico novo em `app/static/js/shell.js` (mesmo padrão do `admin.js::abrirModalCsat`: `.hidden`/`.flex` via `classList`, nunca o atributo `hidden` — Preflight tem especificidade zero nele; trava de scroll em `<html>`+`<body>`; fecha só no clique no *backdrop*, não na imagem ampliada). Miniatura em `usuarios.html` ganhou `data-lightbox="{{ foto }}"` + `cursor-zoom-in` só quando há foto (o círculo de iniciais sem foto não abre nada). **CSS:** classes novas (`cursor-zoom-in/out`, `object-contain`, `max-w-[90vw]`, `max-h-[90vh]`, `bg-navy-900/70`) exigiram `npm run build:css` (Tailwind purga o que não está em uso nos templates — só editar o HTML não bastava). Validado num harness servido pelo próprio `/static` (JS real, CSP real): clique na miniatura abre (`display:flex`, `src` correto, scroll travado), clique na imagem ampliada não fecha, clique no fundo e Esc fecham (scroll destravado). |
| 2026-07-23 | 3.9.1 / Estado | **Upload de foto em `/admin/usuarios` não funcionava (CSP bloqueava o auto-envio) + botão movido pra coluna "Alterar".** Dois problemas reportados pelo usuário com print da tela. **(1) Bug:** o input de arquivo usava `onchange="this.form.requestSubmit()"` inline — a CSP do projeto é `script-src 'self'` sem `'unsafe-inline'` (Seção 3.8, `app/security/headers.py`), que bloqueia **qualquer** atributo `onXXX` inline (diferente de `<script src>` externo); o clique escolhia o arquivo mas nada acontecia depois, sem erro visível pro usuário (só no console do navegador). Fix: trocado por `data-autosubmit` (`app/templates/admin/usuarios.html`) — convenção já existente em `app/static/js/shell.js` (mesma usada nos filtros de `fila.html`/`kanban.html`), que registra o listener via `addEventListener` num `<script>` externo, permitido pela CSP. Validado num harness estático servido pelo próprio app (`/static`, script externo real) confirmando que `data-autosubmit` dispara `requestSubmit()` no `change`. **(2) Layout:** o botão "Enviar/Trocar foto" saiu da coluna "Foto" (que agora só mostra a miniatura) e foi pro topo da coluna "Alterar", junto das demais ações sobre o usuário — pra quem não é TI (Admin de outro setor, Operador do Marketing) essa era a única ação disponível ali, mas a coluna só mostrava um "—" sem nada clicável. |
| 2026-07-23 | doc IA · F3 (Seções 4.4, 5, 7) / Estado | **F3 — Busca de chamados semelhantes (FTS português) citados na nota interna da triagem.** Migration `0053_chamados_fts` (a `0052` foi tomada no mesmo dia por `0052_avatares_admin_marketing`, de outra frente — numeração conferida, regra C1): coluna `chamados.fts tsvector` GENERATED (titulo+descricao, dicionário `portuguese`) + índice GIN, **aplicada em produção via MCP** e validada com busca real no corpus (554+ chamados: "impressora OR não imprime" → 3 legados RESOLVIDOS com resolução registrada). **`app/repositories/ia_busca.py` (novo):** `buscar_semelhantes(conn, ...)` recebe a conexão administrativa do motor; filtro por `departamento_id` **obrigatório no SQL** (defesa em profundidade — RLS não ajuda na conexão admin), só `status='RESOLVIDO'`, exclui o próprio chamado; termos do modelo (`SaidaTriagem.termos_busca`) unidos com OR via `websearch_to_tsquery` (imune a erro de sintaxe com entrada arbitrária); "resolução registrada" = última mensagem pública de staff do chamado semelhante (JOIN LATERAL — decisão da Seção 4.4, sem coluna nova); ranking `ts_rank` + desempate por `resolvido_em`. **Motor (`app/ia/triagem.py`):** busca roda apenas quando a ação é `NOTA_INTERNA` (pergunta pública nunca cita caso interno) e há termos; falha da busca é **silenciosa** (nota sai sem a seção — Regra de Ouro #5); `montar_nota()` ganha a seção "Casos semelhantes já resolvidos" (código + título + resolução normalizada/truncada ~280 chars; omitida sem resultados — degradação graciosa do DoD); códigos citados auditados em `ia_triagens.resultado.semelhantes_codigos`. **Testes:** 8 novos em `test_ia_triagem.py` (nota cita código/resolução; filtro de departamento e consulta OR provados nos args; seção omitida; sem termos não toca no banco; falha silenciosa; PERGUNTAS não busca; truncamento; `montar_consulta`) + 4 e2e com corpus semeado em `tests/e2e/test_ia_busca_fts.py` (marker `rls`: acha o certo, não vaza outro departamento, não-RESOLVIDO fora, resolvido sem mensagem de staff citável sem resolução). Suíte completa verde. **Sombra confirmada ativa em produção** (gestor configurou as envs no Railway — triagens reais de 2026-07-23 em `ia_triagens`, ~US$ 0,002/chamado). |
| 2026-07-23 | 6.1 / Estado | **Sino de notificações não deve mais "apitar"/acender vermelho por SLA estourado — só para CHAMADO NOVO.** Bug reportado pelo usuário: um chamado antigo que só ficou com SLA estourado enquanto já em atendimento mantinha a bolinha vermelha acesa (a lista de `/notificacoes` traz todo não-resolvido, `AtendimentoRepo`/`MensagensRepo.notificacoes()`) e, pior, qualquer `UPDATE` em `chamados` (mudança de status, recálculo de prioridade por SLA do Marketing, atribuição...) chegava via Realtime com `event: "*"` e tocava a animação do sino (`sinalizar()` em `notificacoes.js`) — não só chamado novo de verdade. **Fix em duas frentes, sem tocar a query do backend** (a lista continua trazendo todo pendente, isso é informativo): **(1)** `app/templates/_notificacoes.html` — cada item ganhou `data-notif-novo="1"` só quando `status in ('NOVO', 'RESOLVIDO')` (chamado novo ainda não atendido, ou resolvido aguardando avaliação — as duas únicas situações que já eram "chamado novo" pro usuário; um `EM_ATENDIMENTO`/`AGUARDANDO_*` com SLA estourado não ganha o marcador, mesmo aparecendo na lista). **(2)** `app/static/js/notificacoes.js` — `atualizarDot()` passou a procurar esse marcador em vez de contar qualquer `<a>` da lista; a assinatura Realtime de `chamados` trocou de `event: "*"` para `event: "INSERT"` (só linha nova toca o sino; `mensagens` INSERT continua tocando, é conversa nova de verdade). Teste novo em `test_fase4_fase5.py` (`test_notificacoes_marca_novo_mas_nao_sla_estourado_em_atendimento`): chamado NOVO marcado, chamado `EM_ATENDIMENTO` com SLA vencido aparece na lista mas sem o marcador. |
| 2026-07-23 | 3.9.1 / 5 / Estado | **Admin de qualquer setor e Operador do Marketing passam a poder enviar/trocar a foto de perfil de usuários JÁ CRIADOS — migration `0052_avatares_admin_marketing`.** Pedido do usuário: até aqui só o TI conseguia mexer no perfil de outra pessoa (`perfis_admin_all`, 0012) — foto de outro usuário só dava pra enviar na hora de CRIAR a conta (`criar_usuario`, também exclusivo do TI). **RLS:** nova policy `perfis_update_avatar_staff` (`FOR UPDATE`) libera a LINHA de qualquer usuário para `auth_role()='ADMIN'` (qualquer setor) ou `auth_role()='OPERADOR'` do Marketing — mas a COLUNA continua restrita pelo trigger `enforce_perfil_self_so_avatar` (0033, inalterado): como esses papéis não passam em `auth_is_ti()`, só `avatar_path`/`updated_at` são graváveis na linha alheia, qualquer outra coluna (role/setor/nome/...) segue disparando a exceção. **App:** `admin_context` (`app/routes/admin.py`) ganhou uma 3ª porta de entrada — `is_mkt_operador` (OPERADOR + departamento Marketing) — ao lado de TI e Admin-de-setor; novo campo `AdminCtx.pode_editar_avatares` e guard `_require_pode_editar_avatares` (mais permissivo que `_require_ti`) protegem a rota nova `POST /admin/usuarios/{id}/avatar` (mesmo padrão de upload do `criar_usuario`: Storage via `service_role` key, já que a RLS de `storage.objects` só libera cada um escrever no PRÓPRIO path). `GET /admin/usuarios` trocou o guard de `_require_ti` para `_require_pode_editar_avatares` — quem não é TI vê a lista/miniatura/upload de foto, mas **não** vê "Nova conta", troca de papel, reset de MFA nem exclusão (continuam `_require_ti`, escondidos no template). `AdminRepo.usuarios()` passou a trazer `avatar_path`/`updated_at` para a miniatura (`avatar_url`, mesmo helper já usado em fila/kanban). Navegação (`workspace_base.html`): o submenu "Admin" ganhou a mesma 3ª porta (Operador do Marketing só vê "Contas de usuário", nunca "Gestão de catálogo"); Admin de qualquer setor também passou a ver "Contas de usuário" (antes, só o TI via esse link, mesmo já podendo entrar no painel). Testes: `test_admin.py` — `test_usuarios_restrito_ao_ti` virou `test_usuarios_acessivel_a_qualquer_admin_de_setor_mas_sem_acoes_de_ti` (200 em vez de 403, sem as ações de TI) + 5 novos (Operador fora do Marketing barrado em GET/POST, Operador do Marketing e Admin de outro setor conseguem trocar avatar de outro usuário, tipo de arquivo inválido rejeitado sem chamar o Storage). |
| 2026-07-23 | doc IA · Seções 7 (DoD), 10.2 / Estado | **Avaliação 1–5 ★ da nota interna da IA pelos atendentes (fonte do KPI "notas úteis ≥ 70%") + progresso do doc IA preenchido.** Migration `0051_ia_triagens_avaliacao` — colunas `avaliacao smallint CHECK (1..5)`, `avaliado_por uuid REFERENCES perfis` e `avaliado_em timestamptz` em `ia_triagens` — **aplicada em produção via MCP** (pós-check: 3 colunas criadas; RLS intacta — 1 policy SELECT, zero de escrita, invariante da 0050 preservada). **Backend:** `AtendimentoRepo.ia_triagem_nota()` (última triagem `NOTA_INTERNA` do chamado + avaliação atual, lida sob RLS — quem não é staff do departamento recebe `None`) e `AtendimentoRepo.avaliar_ia_triagem()` (o escopo é provado lendo a triagem sob a RLS do avaliador ANTES da escrita pela `admin_connection()` — `ia_triagens` continua sem policy de escrita; reavaliar sobrescreve, `avaliado_por`/`avaliado_em` auditam quem/quando); fachada `ChamadosRepo` delega. **Rota:** `POST /workspace/chamados/{id}/ia/avaliacao` (CSRF, nota fora de 1–5 → 400, triagem fora do escopo/id trocado → 404, sucesso → redirect 303). **UI:** bloco "Pré-análise da IA" em `workspace/atendimento.html` (aparece só quando há triagem visível): 5 estrelas clicáveis (form POST, uma por botão), estado atual preenchido em âmbar, "Avaliada com N/5 por Fulano — clique para reavaliar". **Testes:** 6 novos em `test_workspace.py` (bloco presente/ausente, avaliação existente, grava e redireciona, 400 fora da faixa, 404 fora do escopo) + `ia_triagem`/`avaliar_ia_triagem` no FakeRepo; suíte completa verde. **Doc IA atualizado (Seção 9):** DoD da F0 fechado (✅ concluída — migration em produção, RLS provada ao vivo, perfil "Assistente IA" promovido), F1 com os 5 itens de código/teste marcados (pendências: envs no Railway + 20 chamados em sombra + p95), F2 marcada como código completo (pendências: revisão de tom pelo gestor + verificação Realtime, ambas atrás do gate sombra→perguntas), Seção 10.2 com a query do KPI (`avaliacao >= 4` = útil), Tabela de Estado com a linha nova. |
| 2026-07-23 | doc IA · Seção 2.3 (fluxo) / Estado | **Guarda de atendimento relaxada: a nota interna da rodada 1 sempre chega — atendente assumir o chamado não suprime a pré-análise.** Motivo (validação local da sombra): no BOND-2026-00580 o operador assumiu 15 s após a abertura e a guarda antiga (`status='NOVO'` e `operador_id IS NULL` abortava a triagem inteira) descartava a nota — justamente a informação de quem atende. **Novo comportamento** (`app/ia/triagem.py`): rodada 1 tria em qualquer status exceto `RESOLVIDO`; "atendimento iniciado" agora **força a ação a `NOTA_INTERNA`** via `decidir_acao(..., atendimento_iniciado=)` (nunca interpela o autor com humano atuando) e segue vetando re-triagem (rodada > 1); a guarda é **reavaliada com estado fresco** após a chamada de modelo (se o atendente assumir durante a espera, a pergunta pública degrada para nota interna — fecha a corrida de 15 s). Doc atualizado ANTES do código (Seção 9, regra de precedência). Testes: 3 novos + matrix de `decidir_acao` estendida em `test_ia_triagem.py` (rodada 1 com atendente gera nota; fora da sombra degrada sem e-mail; RESOLVIDO não tria); suíte completa **396 verdes**. **Diagnóstico do dia (ambiente local):** a triagem "não rodava" por duas causas empilhadas — (a) `.env` local sem `IA_TRIAGEM_ATIVA`/`IA_TRIAGEM_DEPARTAMENTOS` (adicionadas); (b) porta 8000 servida por um uvicorn zumbi do Python do sistema (sem deps do projeto) disputando com a instância do `.venv` — saneado matando ambos e subindo instância única do `.venv`. |
| 2026-07-22 | doc IA · C5 executada / F1 validação | **Troca Groq→OpenAI concluída no `.env` local + primeiro chamado real triado em produção (sombra).** (a) Perfil "Assistente IA" promovido em produção (o SQL do gestor criou o usuário mas a promoção não tinha aplicado — rodada via MCP com `perfis_self_so_avatar` desabilitado só na transação). (b) **BOND-2026-00578 triado de verdade** (motor manual, env local): linha em `ia_triagens` (rodada 1, UNICO, NOTA_INTERNA, 1.163/234 tokens, 1.515 ms), nota interna assinada "Assistente IA" com 3 perguntas registradas (sombra), evento `IA_TRIAGEM`; **RLS provada ao vivo** (claims do autor: 0 notas internas, 0 linhas de `ia_triagens` visíveis). (c) `.env` local migrado para os nomes canônicos `IA_TRIAGEM_*` com chave OpenAI (colada pelo usuário direto no arquivo; chave Groq removida — provedor descontinuado). (d) **Fix no cliente:** GPT-5.x rejeita `max_tokens` (400 `unsupported_parameter`) — `app/ia/cliente.py` passa a enviar `max_completion_tokens` (aceito também pelo Groq); `temperature=0.2` testada e aceita. (e) **Comparação de modelos (DoD F1)** no mesmo chamado (BOND-2026-00577, dry-run): `gpt-5.4-mini` (2,7 s · US$ 0,0018 · validou categoria/prioridade, próximo passo concreto) ≻ `llama-3.3-70b` (1,5 s · genérico). Decisão C5 confirmada na prática. **Pendente:** envs no Railway (painel — MCP sem acesso) + deploy para o disparo automático em produção. |
| 2026-07-22 | doc IA · F1 extra + F2 (Seções 2.3, 7) / Estado | **F1: perfil "Assistente IA" e testes extras · F2: perguntas ao usuário + re-triagem implementadas.** **(a) Perfil de serviço (C4):** criação via MCP foi bloqueada pelo classificador de permissões — SQL pronto entregue ao gestor para rodar no SQL Editor (INSERT em `auth.users` + promoção com `perfis_self_so_avatar` desabilitado só na transação, padrão do e2e). Railway MCP sem acesso ao projeto — env vars ficam no checklist manual. **(b) F1 testes extras:** 3 testes de hook em `test_portal.py` (abertura TI agenda task; depto fora da lista e kill switch não agendam — o `from app.config import get_settings` local de `responder_chamado` foi removido por ofuscar o patch e ser redundante), prompt carregado sem cabeçalho, histórico com rodada/passe/sombra, schema estrito (>3 perguntas recusadas), `custo_usd` e `json_mode` no cliente. **(c) F2 motor (`app/ia/triagem.py`):** máquina de rodadas **derivada do banco** — rodada = MAX(ia_triagens.rodada)+1; re-triagem exige última ação `PERGUNTAS` **e** mensagem pública do AUTOR posterior a ela (replay/duplicata não re-tria); teto `IA_TRIAGEM_MAX_RODADAS` (3ª rodada impossível — teste); `decidir_acao()` pura: PERGUNTAS só fora da sombra + insuficiente + confiança ALTA + rodada < teto (senão nota interna; na última rodada, nota com lacunas sinalizadas). Ação PERGUNTAS: mensagem PÚBLICA (`_enviar_pergunta_publica`, `is_interna=false` fixado — único caminho público do motor) + e-mail ao autor via `notificar_nova_mensagem_email` (Reply-To inbound; falha de e-mail não derruba a triagem — teste); conversa pública completa entra no contexto da rodada 2 (prompt instrui a não repetir pergunta respondida). **(d) Hooks de re-triagem:** `portal.py::responder_chamado` (só quando remetente = autor) e `common.py` inbound (só `is_client`; SELECT do chamado ganhou `d.nome AS departamento`), ambos via `background_tasks` + `deve_triar`. Testes: 12 novos em `test_ia_triagem.py` (matrix de `decidir_acao`, fluxo PERGUNTAS com e-mail, sombra nunca pública, re-triagem com conversa, sem resposta não roda, teto, nota com lacunas, guarda de atendimento na re-triagem) + 3 hooks em `test_portal.py`. Suíte completa **391 verdes** + 16 skips; ruff/mypy limpos. Sombra→perguntas em produção continua atrás de env (`IA_TRIAGEM_MODO_SOMBRA=false` só após validar a sombra — critério F1→F2). |
| 2026-07-22 | doc IA · F1 (Seções 2.2, 2.3, 7) / Estado | **F1 — Motor de triagem por IA (agente TI, modo sombra) implementado.** Migration `0050` **aplicada em produção** via MCP (pós-check: RLS on, 1 policy SELECT, zero de escrita; advisors sem alerta novo). **Motor** (`app/ia/triagem.py`): background task na abertura → guarda de atendimento (`status='NOVO'` e `operador_id IS NULL`) → verificação prévia de idempotência → **uma chamada estruturada** (`json_mode`/`response_format`, saída validada por `app/ia/schemas.py::SaidaTriagem`; 1 retry em JSON inválido; falha de provedor sem retry) → persistência atômica: linha em `ia_triagens` (`ON CONFLICT DO NOTHING` sobre o UNIQUE — corrida/retry não duplica nota) + **nota interna** assinada pelo perfil "Assistente IA" (lookup por nome com cache; `is_interna=true` **fixado no SQL** da função de persistência, que nem aceita o parâmetro — invariante 8.2 testada por assinatura) + evento `IA_TRIAGEM` em `historico_chamados`. Custo/tokens/duração gravados (`PRECOS_USD_POR_MTOK` + `custo_usd()` em `app/ia/cliente.py`). Kill switch: sem `IA_TRIAGEM_ATIVA`/chave, zero efeito colateral (nem HTTP nem banco — teste). **Prompt mestre TI** versionado em `app/ia/prompts/ti.md` (higiene epistêmica, anti-injection, contrato JSON; cabeçalho de doc não vai ao modelo). **Hook** em `app/routes/portal.py::criar_chamado`: `tarefa_ia` vira `BackgroundTasks` — resumo do Químico intocado (C2) + `triagem.executar_triagem` quando `triagem.deve_triar(depto destino)`. Testes: `tests/test_ia_triagem.py` novo (15 casos: kill switch, guardas, fluxo sombra completo com custo, JSON inválido→retry→ERRO, provedor fora→ERRO silencioso, idempotência dupla, perfil ausente, invariante de assinatura, funções puras). Suíte completa 365 verdes + 16 skips; ruff/mypy limpos. **Sombra ainda desligada em produção** — ligar = env no Railway (`IA_TRIAGEM_ATIVA=true`, `IA_TRIAGEM_DEPARTAMENTOS=TI`, chave OpenAI); critério de saída da F1 (20 chamados reais avaliados) só corre depois disso. |
| 2026-07-22 | doc IA · F0 (Seções 1, 2.4, 4.1, 4.2) / Estado | **F0 — Fundação da frente de IA de triagem (plano_md_mestre_IA.md).** (1) **Migration `0050_ia_triagens`** (numeração conferida — C1): tabela de auditoria/idempotência por rodada+passe (`UNIQUE (chamado_id, rodada, passe)`), RLS habilitada com SELECT só para staff no escopo do departamento de destino do chamado (mesmo escopo de `historico_chamados` pós-`0020`; autor nunca lê) e **nenhuma** policy de escrita (INSERT/UPDATE exclusivos da `admin_connection()`). Idempotente; **ainda não aplicada em produção** (aguarda go do gestor). (2) **Flags `IA_TRIAGEM_*`** (Seção 2.4 do doc IA, todas de uma vez, defaults desligados: `ATIVA=false`, `MODO_SOMBRA=true`, key vazia) em `app/config.py` + `.env.example`; **rename `groq_*` → `ia_triagem_*`** (C5) com `[DECISÃO DE ENGENHARIA]` fallback de alias (`AliasChoices`) para os envs antigos `GROQ_*` — produção com envs antigos continua funcionando até o gestor migrar os nomes no Railway; defaults novos apontam OpenAI/gpt-5.4-mini. (3) **Esqueleto `app/ia/`** com `cliente.py` (C2): chamada compatível-OpenAI extraída de `ia_resumo.py`, provedor-agnóstica, devolve texto+tokens (`usage`) para o custo auditável da F1; `ia_resumo.py` passa a consumi-la sem mudança de comportamento, herdando o timeout unificado de 30 s (C6, antes 20 s). (4) **Perfil de serviço "Assistente IA"** documentado em `supabase/registro_usuarios.sql` (C4 — usuário de serviço no Auth + promoção OPERADOR/depto NULL; criação no painel pendente do gestor). Testes: `test_config.py` (+5: defaults desligados, alias GROQ, precedência, CSV de departamentos), `test_ia_resumo.py` adaptado aos novos seams (mesmo contrato; +asserção do timeout C6), `test_ia_cliente.py` novo (3 casos), `tests/e2e/test_rls_ia_triagens.py` novo (5 casos, marker `rls`: staff lê só o escopo, autor não lê, `authenticated` não escreve, UNIQUE anti-duplicação). Suíte completa 350 verdes + 16 skips (e2e sem Docker); ruff/mypy limpos. |
| 2026-07-22 | 5.1 / 3.2 | **Químico: tela de abertura vira só o formulário dinâmico — "Categoria" vira "Formulários", Assunto/Prioridade/Descrição saem da tela.** Pedido do usuário: quando o departamento de destino é o Químico, os 3 campos genéricos (Assunto, Prioridade, Descrição detalhada) somem — o layout dinâmico da categoria (`_campos_quimico.html`) já cobre tudo — e o rótulo "Categoria" vira "Formulários" (mais preciso: são os 3 formulários do setor). **Problema a resolver:** Assunto/Descrição são `required` no HTML e exigidos pelo servidor (`Form(...)` + checagem explícita) — escondidos via `display:none` continuam bloqueando o envio nativo do form (a validação HTML5 não isenta campo obrigatório só por estar sem `display`; a barra de progresso trava o clique em "Abrir chamado" sem mensagem visível). **Solução:** `app/static/js/novo_chamado.js` — além de esconder os blocos (`campo-assunto`, `campo-descricao`, `campo-prioridade` também escondido pro Químico, mantendo o valor padrão MEDIA), tira o `required` dos dois inputs quando o departamento é o Químico (`tituloInput.required = !quimico`); rótulo "Categoria" (`#categoria-label`, novo id em `novo_chamado.html`) trocado por `textContent`. **Servidor deriva os dois campos:** novo `app/domain/formularios_quimico.py::titulo_e_descricao_automaticos(nome_categoria, dados)` — por categoria, usa um campo "identificador" (cliente) pro Assunto (`"{categoria} — {identificador}"`) e um campo "narrativa" pra Descrição (ex.: `descricao_situacao` no Registro de Ocorrência, `objetivo_visita` na Visita Técnica, `descricao_amostra` na Análise Laboratorial) — cai pro nome da categoria se o campo-fonte vier vazio, nunca retorna string vazia pra uma categoria do Químico. `app/routes/portal.py::criar_chamado`: o bloco de validação do formulário dinâmico (já existente) foi movido pra ANTES da checagem "assunto/descrição obrigatórios", e agora sobrescreve `titulo`/`descricao` com o resultado da função nova quando `eh_quimico` — a checagem seguinte vira uma rede de segurança (sempre passa pro Químico, seu comportamento não muda pros demais departamentos). Testes: 6 novos em `test_formularios_quimico.py` (função nova, incl. fallback e categoria fora do Químico → `("", "")`) + 1 novo em `test_portal.py` (`test_criar_quimico_sem_assunto_e_descricao_deriva_automaticamente`, simula o POST real sem esses dois campos preenchidos). **Corrigido de quebra:** `tests/test_portal.py::_abertura_ocorrencia` ainda usava valores livres para `regiao`/`supervisor`/`gerente`/`tipo_ocorrencia`/`produto` (`"Sul"`, `"Maria"`, `"Vazamento"` etc.) de antes desses campos virarem `select` (entrada anterior) — o teste `test_criar_quimico_grava_dados_formulario` estava quebrado (400 em vez de 303) sem eu ter notado na hora, porque a suíte inteira só roda com `.venv/Scripts/python.exe` (o `python` do PATH não tem `fastapi`/`asyncpg`) — descoberto e corrigido ao rodar a suíte certa antes de fechar esta tarefa. Validado num harness estático no browser carregando o `novo_chamado.js` real (toggle completo: rótulo, `display`, `required`, ida e volta entre departamentos). |
| 2026-07-22 | 5.1 / 3.2 | **Químico: campo "Subcategoria" escondido na abertura de chamado.** Pedido do usuário — as 3 categorias do Químico (0049) nunca tiveram subcategoria (o backend já tratava isso: `subcategoria_id` não é exigido nem gravado para elas, `app/routes/portal.py::criar_chamado`), mas o `<select>` de Subcategoria continuava visível em `portal/novo_chamado.html`, sempre parado em "Escolha a categoria primeiro" — campo morto na tela. `app/static/js/novo_chamado.js`: reaproveitado o mesmo padrão já usado para mostrar/esconder o bloco de campos dinâmicos (`ehQuimico()`) — o `<label id="campo-subcategoria">` (novo id) agora escondido via `style.display` quando o departamento de destino é o Químico, visível para os demais. Nenhuma mudança de backend: a rota já não exige nem grava subcategoria para essas categorias. Validado no browser com um harness estático carregando o `novo_chamado.js` real (fora do fluxo automatizado de testes, que não cobre JS de template). |
| 2026-07-22 | 5.1 / Estado | **Químico: último dropdown do Registro/Visita Técnica convertido — "Região do cliente" usa a mesma lista de "Região".** Usuário confirmou que "Região do cliente" (Solicitação de Visita Técnica) é o mesmo dropdown que "Região" (Registro de Ocorrência) — `app/domain/formularios_quimico.py`: campo `regiao_cliente` trocado de `tipo="text"` para `tipo="select"`, reaproveitando `_REGIOES`. Com isso **não sobra nenhum dropdown como texto livre** nos 3 layouts do Químico — comentário do topo do módulo atualizado. `tests/test_formularios_quimico.py`: os 3 usos de `regiao_cliente` nos fixtures/testes trocados para valor válido da lista (incluindo o teste que isolava "unidade inválida" — precisava de um `regiao_cliente` válido pra não falhar antes por outro campo). |
| 2026-07-22 | 5.1 / Estado | **Químico: Gerente, Tipo de Ocorrência e Produto também viram `select`.** Segue a entrada anterior (Região/Supervisor) — usuário anexou os prints dos 3 dropdowns restantes do Registro de Ocorrência: "Gerente" (4 opções), "Tipo de Ocorrência" (PRODUTO/PROCESSO/NÃO IDENTIFICADA) e "Produto" (66 opções, catálogo de produtos Bondmann). `app/domain/formularios_quimico.py`: novas constantes `_GERENTES`/`_TIPOS_OCORRENCIA`/`_PRODUTOS`; os 3 campos trocados de `tipo="text"` para `tipo="select"`. Com isso, **todos os dropdowns do Registro de Ocorrência estão com a lista real** — só "Região do cliente" (Solicitação de Visita Técnica) segue texto livre, sem lista informada. "VENDA DIRETA" aparece como opção em Região, Supervisor **e** Gerente (mesma designação de canal direto, confirmado pelo usuário). ⚠️ a lista de Produto não está em ordem alfabética estrita no material original (ex.: as 3 variantes de "DESINFETANTES BONDMANN" aparecem fora de ordem) — transcrita literalmente na ordem do form, sem reordenar. `tests/test_formularios_quimico.py`: fixture de payload válido trocada para opções reais desses 3 campos. |
| 2026-07-22 | 5.1 / Estado | **Químico: Região e Supervisor viram `select` com as listas reais do form.** Segue a entrada anterior — o usuário anexou os prints do dropdown "Região" (114 opções, códigos `NNN-CIDADE` + "VENDA DIRETA") e "Supervisor" (24 opções, nomes + "VENDA DIRETA") do FB033-Registro de Ocorrências. `app/domain/formularios_quimico.py`: novas constantes `_REGIOES`/`_SUPERVISORES`, campos `regiao`/`supervisor` trocados de `tipo="text"` para `tipo="select"` (com validação de opção via `validar_payload`, igual aos demais `select` do schema); comentário do topo do módulo atualizado (só Gerente/Tipo de Ocorrência/Produto/Região do cliente seguem texto livre, pendentes das próximas listas que o usuário vai anexar). ⚠️ a lista de Região tem lacunas na numeração dos códigos no material recebido (ex.: nada entre 106 e 125) — mantida como está (poderiam ser regiões descontinuadas); a confirmar com o usuário se falta algo antes de considerar definitiva. `tests/test_formularios_quimico.py`: fixture de payload válido trocada de valores livres ("Sul"/"Maria") para opções reais das novas listas. |
| 2026-07-22 | 5.1 / Estado | **Químico: schema com os campos reais dos 3 Microsoft Forms + migration `0049` aplicada em produção.** Segue a entrada de 2026-07-21 abaixo — o usuário anexou os prints dos 3 formulários reais (FB033-Registro de Ocorrências, Solicitação de Visita Técnica, Solicitação de Análise Laboratorial), substituindo o schema provisório em `app/domain/formularios_quimico.py` pelos campos exatos (23 campos no total entre as 3 categorias). **Novidades no schema:** `min_chars` (Lote ≥13, Fone ≥10), tipos `email`/`tel` (validação de formato), `checkbox_multi` (múltipla escolha — "Análises solicitadas" da Análise Laboratorial, 8 opções) — `validar_payload`/`valores_para_template` passaram a receber `dict[str, list[str]]` (não mais `dict[str,str]`) porque `checkbox_multi` manda várias entradas com o mesmo `name`; o agrupamento em `app/routes/portal.py::criar_chamado` mudou de dict simples para `dict[str, list[str]]` (um dict simples perderia todas as marcações menos a última). As 7 perguntas "Sim/Não/Outra" de Análise de Causa Provável (Ocorrência) foram simplificadas de "checkbox até 2 opções" para `select` (Sim/Não/Outra) + campo de detalhe opcional — mais direto de preencher/validar sem perder a informação. **Campos convertidos para texto livre** (Região, Supervisor, Gerente, Tipo de Ocorrência, Produto, Região do cliente): os dropdowns desses campos apareciam fechados nos prints ("Selecionar sua resposta"), sem lista de opções visível — virar `<select>` sem opções seria pior que texto livre; documentado no topo do módulo, troca de uma linha quando o setor informar a lista real. Upload de anexos ("Fotos e/ou Vídeos") não virou campo dinâmico — reaproveita o mecanismo de anexos já existente em todo chamado. **Migration `0049` aplicada no projeto Supabase de produção** (`iurlzlhbnoemkzgexcfk`) via MCP, após conferir que "Dpto Químico" existia com `recebe_chamados=false` (0027) e que as colunas novas ainda não existiam — idempotente, sem impacto nos 554 chamados existentes. `get_advisors` conferido pós-migration: nenhum alerta novo introduzido. **Correção de segurança:** a `GROQ_API_KEY` fornecida pelo usuário tinha sido colocada em `.env.example` (arquivo versionado) em vez de `.env` (gitignored) — movida para o lugar certo antes de qualquer commit, `.env.example` restaurado a placeholder vazio. Testes: `test_formularios_quimico.py` reescrito (20 casos), `test_ia_resumo.py`/`test_portal.py` ajustados aos nomes de campo reais e ao formato multi-valor (incluindo um teste dedicado ao `checkbox_multi` simulando múltiplos checkboxes com o mesmo `name`). Suíte completa + ruff/mypy verdes. |
| 2026-07-21 | 5.1 / 3.2 / Estado | **Departamento Químico: abertura de chamado DINÂMICA por categoria + resumo por IA (nota interna) — migration `0049_departamento_quimico`.** Pedido do usuário: o setor Químico passa a **receber** chamados de três naturezas, cada uma com um layout de formulário diferente (campos originados de 3 Microsoft Forms), e a categoria escolhida troca dinamicamente os campos; além disso uma IA resume o chamado + campos e devolve uma nota interna. **(1) Schema/depto:** `0049` faz `UPDATE departamentos SET recebe_chamados=true` no já-existente **"Dpto Químico"** (`0027`) — não cria setor novo; semeia 3 categorias (`Registro de Ocorrência`, `Solicitação de Visita Técnica`, `Solicitação de Análise Laboratorial`, sem subcategoria); adiciona `chamados.dados_formulario jsonb` (respostas `{name: valor}`), `chamados.resumo_ia`/`resumo_ia_em`. RLS de atendimento é genérica por `departamento_id` (`0008`) — nenhuma policy nova. **(2) Layout por categoria em código:** `app/domain/formularios_quimico.py` (`CAMPOS_POR_CATEGORIA` + `validar_payload`/`rotular`); campos **provisórios** a substituir pelos reais dos forms (org login Microsoft — não acessíveis ao agente). **(3) Abertura dinâmica:** espelha a cascata de subcategorias — nova `GET /portal/chamados/campos` → partial `portal/_campos_quimico.html`; container `#campos-dinamicos` em `novo_chamado.html` com `hx-trigger="change from:#categoria-select"`; visibilidade por depto em `novo_chamado.js` (padrão do bloco Marketing). `POST /portal/chamados` lê `campo__*` do multipart, valida contra o schema (defesa em profundidade — só chaves conhecidas) e grava `dados_formulario` (`AtendimentoRepo.criar`/`obter` estendidos; `CatalogoRepo.nome_categoria` novo). **(4) IA (Groq):** `app/services/ia_resumo.py` chama a API compatível-OpenAI (config plugável `groq_*` em `app/config.py`/`.env.example`; **vazio ⇒ desligado**, abertura nunca falha), rodada como `starlette.background.BackgroundTask` anexada ao redirect (o parâmetro `BackgroundTasks` do FastAPI não resolve sob `@limiter.limit`+`from __future__ annotations`, mesma limitação do `UploadFile` já documentada); grava via `admin_connection()`. Nota exibida **só ao staff** (`workspace/atendimento.html`, fundo âmbar); `dados_formulario` renderizado nos dois detalhes (`portal/chamado_detalhe.html` + workspace). Testes: `test_formularios_quimico.py`, `test_ia_resumo.py` (Groq/banco mockados) + fluxo Químico em `test_portal.py` (fixture de rate-limit desliga `@limiter.limit` no arquivo). 324 verdes, ruff/mypy limpos. **Pendências do usuário:** colar os campos reais dos 3 forms; aplicar a `0049` no Supabase; definir `GROQ_API_KEY`. |
| 2026-07-21 | 5 / Estado | **Oito correções/melhorias pedidas pelo usuário: avaliação pendente, categoria editável, link quebrado, modal de CSAT, indicadores mensais, Fila/Kanban por setor sem fila, e-mail de boas-vindas, dado de importação legada.** **(1) Avaliação pendente:** `GET /portal/chamados/novo` (`app/routes/portal.py::novo_chamado_form`) agora chama `ChamadosRepo.avaliacao_pendente()` (novo, `AtendimentoRepo.avaliacao_pendente` — chamado RESOLVIDO mais antigo do próprio autor sem `avaliacao_nota`) e redireciona pro detalhe desse chamado (`?avaliar_pendente=1`, banner em `portal/chamado_detalhe.html`) em vez do formulário de abertura. **(2) Categoria/subcategoria editável:** novo bloco "Categoria"/"Subcategoria" nas Ações do Workspace (`workspace/atendimento.html`), `POST /workspace/chamados/{id}/categoria` (`AtendimentoRepo.alterar_categoria`, valida o par categoria/departamento e categoria/subcategoria como na abertura) + `GET .../subcategorias` (cascade HTMX). `AtendimentoRepo.obter()` passou a selecionar `categoria_id`/`subcategoria_id` (faltavam no SELECT). **(3) Link morto em "Últimas avaliações":** `admin/dashboard.html` tinha `<a href="#">`; `AdminRepo.avaliacoes_recentes()` passou a trazer `c.id` e o link aponta pra `/workspace/chamados/{id}`. **(4) Modal do CSAT:** clicar numa barra de "Distribuição do CSAT" (`admin.js`, Chart.js `onClick`) abre um modal (vanilla JS — nenhuma outra tela do admin usa Alpine hoje) com os chamados daquela nota, scroll + filtro de texto, via `GET /admin/indicadores/avaliacoes` (novo `AdminRepo.chamados_por_nota`). **(5) Indicadores mensais:** `GET /admin` ganhou `?periodo=YYYY-MM` (default mês corrente); todo KPI/gráfico/avaliação do `AdminRepo` ganhou `periodo_inicio`/`periodo_fim` (filtro em `created_at`, nada é sobrescrito — é sempre um recorte ao vivo). Seletor `input type="month"` + setas ‹ › no topo do painel. **(6) Fila/Kanban só pra quem atende:** `ChamadosRepo.perfil()` passou a trazer `recebe_chamados` (flag do departamento do próprio usuário); `workspace_base.html` esconde os links "Fila de chamados"/"Kanban" e `fila_lista`/`kanban` (`app/routes/workspace.py`) redirecionam pra `/portal` quando `recebe_chamados=false` — alvo real: ADMIN líder de setor sem fila (Controladoria, Financeiro etc.), que hoje só abre/acompanha. **(7) E-mail de boas-vindas:** `POST /admin/usuarios` (`criar_usuario`) dispara `notificar_novo_usuario_email` (novo, `app/notification.py`, mesmo estilo HTML de `notificar_nova_mensagem_email`) via `BackgroundTasks`, instruindo o primeiro acesso por `/esqueci-senha`. **(8) Chamados legados sem operador:** conferido contra a planilha original — os 471 chamados `[Legado #...]` **já estavam todos importados** (não era um problema de importação); `#18150` já tinha operador (Osvaldo Bello); `#18134`/`#18070` estavam sem `operador_id` (por isso não apareciam em "Meus chamados"/produtividade) — corrigido via `UPDATE chamados` direto no Supabase, atribuindo a Osvaldo Bello. Testes: fakes de `test_workspace.py`/`test_admin.py`/`test_portal.py` atualizados pra nova superfície dos repositórios + casos novos para os 8 itens. |
| 2026-07-20 | 2.2 / 3.2 / Estado | **Kanban com contagem divergente do total, e-mail ausente para parte dos usuários, filtro na tela de contas.** Três correções pedidas pelo usuário. **(1) Kanban:** `workspace/kanban.html` mostrava `{{ colunas[s]\|length }}` no cabeçalho de cada coluna — contagem do array já em memória, que `FilaRepo.fila()` trunca com `LIMIT 200` **sobre o resultado combinado de todos os status** (não por coluna); com mais de 200 chamados no escopo, os cabeçalhos batiam com o que coube no limite, não com o total real. A rota já buscava a contagem certa (`FilaRepo.fila_stats()`, sem LIMIT, mesma função que alimenta os cards de `workspace/fila.html`) e já passava `stats` pro template — só não estava sendo usada nos cabeçalhos. Trocado para `{{ stats[s] }}`, mesma fonte de verdade da lista. **(2) E-mail ausente:** `app/routes/admin.py::_emails_por_id()` chamava `client.auth.admin.list_users()` sem paginar — o GoTrue pagina a resposta (100 por padrão) e usuários fora da primeira página nunca entravam no mapa `{user_id: email}`, aparecendo como "—" na tela. Corrigido para percorrer todas as páginas (`per_page=200`, laço até página vazia). **(3) Filtro em `/admin/usuarios`:** novo form GET (busca por nome/e-mail, papel, setor — mesmo padrão `data-autosubmit` de `workspace/fila.html`/`kanban.html`) com filtragem em memória na rota (a lista de usuários da organização é pequena, sem paginação de banco); links de confirmação/cancelamento de exclusão passam a preservar os filtros ativos na querystring. |
| 2026-07-20 | 3.2 / 3.3 / Estado | **Autoatendimento (0038/0042) generalizado para TODOS os setores, TI incluído — migration `0047_autoatendimento_todos_setores`.** Pedido do usuário: o TI não conseguia abrir chamado útil para si mesmo — ficava de fora do Kanban/fila (`FilaRepo.fila` só mostra pedido "de fora do setor", salvo exceção) e o autor não podia se autoatender. Caso concreto: a diretoria quer abrir um chamado (ex.: para o TI) sem entrar na plataforma como staff; alguém do próprio TI abre em nome da demanda e ela precisa aparecer no quadro do setor, igual já acontecia no Marketing/RH. `UPDATE departamentos SET autoatendimento = true` (cobre os setores já cadastrados) + `ALTER COLUMN ... SET DEFAULT true` (setor novo, cadastrado depois via `/admin/departamentos`, já nasce com a regra). **Sem policy SQL nova:** `chamados_update_staff`/`mensagens_insert` (`0042`) já liam `departamentos.autoatendimento` de forma genérica, sem nome de setor hardcoded — só a coluna de dados mudou. `app/repositories/{atendimento,fila}.py` (docstrings) atualizados para não afirmarem mais "só Marketing/RH". `tests/e2e/`: `test_ti_sem_flag_autoatendimento_nao_pode_se_autoatender` (esperava bloqueio) virou `test_excecao_ti_autoatendimento` (mesmo padrão positivo de Marketing/RH); fixture `chamado_ti_sem_auto` renomeada para `chamado_ti_auto`. Nenhuma rota/template mudou — a UI (`workspace/atendimento.html`, dropdown de responsável) já era condicionada só pela flag do banco, nunca por nome de setor. |
| 2026-07-17 | 5.1 / Estado | **Parágrafos legíveis no chat + coluna de conversa mais larga.** Mensagens de chat vinham de fontes com hábitos de quebra de linha diferentes (1 Enter, vários seguidos, texto colado de Word/WhatsApp com largura fixa) e sempre renderizaram como um único bloco `whitespace-pre-line` — texto corrido colado com quebra manual saía com linhas curtas artificiais em vez de reformatar na largura real do balão. Nova `app/templating.py::paragrafos_mensagem()` (função pura, testável, registrada como global do Jinja) separa o texto em blocos por linha(s) em branco; dentro de um bloco, se nenhuma linha tem marcador de lista/tópico (`-`, `*`, `•`, `1.`, `a)` etc. — regex `_MARCADOR_LISTA`), as linhas são rejuntadas com espaço para o navegador quebrar sozinho na largura do chat; blocos com marcador preservam a quebra linha a linha (cada item da lista continua distinto). `portal/_mensagens.html` passa a iterar `paragrafos_mensagem(m.conteudo)` num `<p>` por parágrafo (antes: um `<p>` só com o texto cru). Anexos de imagem passam de lista vertical (`space-y-1`) para `flex flex-wrap gap-2` (lado a lado). Coluna de conversa em `workspace/atendimento.html` ganha mais espaço frente ao painel de Ações (`lg:grid-cols-3`→`4`, coluna principal `col-span-2`→`3`, ou seja 2/3→3/4 da largura). `npm run build:css` rodado (diff em `app.css`). |
| 2026-07-17 | 5.1 / Estado | **Miniatura de imagem nos anexos do chat.** Anexos com `mime` (já validado no upload — Seção 3.9) começando por `image/` renderizam uma miniatura clicável 64×64 (`object-cover`, abre a signed URL em nova aba) em vez do link genérico "📎 nome do arquivo"; anexos não-imagem e casos sem URL disponível mantêm o comportamento anterior. Mudança isolada em `portal/_mensagens.html` (compartilhado Portal/Workspace, mesmo padrão da foto de perfil no chat de 2026-07-10). |
| 2026-07-17 | — (chore) | **Remoção do template morto `app/templates/admin/admin_base.html`.** Nenhuma das 4 páginas do painel admin (dashboard, dashboard_marketing, gestao, usuarios) estende esse arquivo — todas estendem `workspace/workspace_base.html` há tempos; o template morto já tinha induzido um bug real (banner adicionado no shell errado, corrigido em sessão anterior). Confirmado que nenhuma rota o renderiza antes de remover. `npm run build:css` após a remoção purga a única utility Tailwind que só existia ali (`.bg-brandgreen-700`, sem uso vivo — as variantes `text-`/`hover:` seguem em uso). Consolidado no `develop` via merge (`3363bd9`). Suíte pytest completa verde. |
| 2026-07-16 | 3.4.1 (nova) / Estado | **Sprint 2 do plano de melhorias, item 2.8/B6 — política de senha + avaliação de MFA.** Hashing confirmado como delegado por completo ao GoTrue (bcrypt gerenciado pela plataforma, sem parâmetro exposto — "o plano de hashing já redigido" da auditoria é essa própria delegação, registrada explicitamente agora). **Política de senha:** mínimo 8 caracteres, sem exigência de composição (NIST 800-63B — comprimento em vez de regra de composição arbitrária). Já era o comportamento real da aplicação em dois pontos (`/redefinir-senha`, `/admin/usuarios`), cada um com sua própria constante `8` — consolidado em `app/security/password_policy.py::SENHA_MIN_CHARS` (novo módulo, fonte única, mesma classe de duplicação já corrigida noutros itens do plano). `supabase/config.toml` (stack local) alinhado (`minimum_password_length` 6→8). **Pendência de ação manual:** o projeto Supabase hospedado segue no default `6` — não há Management API exposta via MCP para automatizar; ajuste fica para o gestor no painel (Authentication → Policies), mesmo padrão dos itens 0.4/2.4. **MFA:** avaliado, não implementado — GoTrue já suporta TOTP nativo sem trocar de provedor, mas o custo real está nas telas de enrollment/challenge que faltam construir; faseamento registrado na Seção 3.4.1 (nova) do plano mestre: Fase 1 (alvo Sprint 3) TOTP opcional para staff, Fase 2 (alvo Sprint 4) obrigatório para ADMIN. Suíte completa (exceto `tests/e2e`) e `ruff app/` verdes; nenhum template tocado. |
| 2026-07-16 | 6.3 / Estado | **Sprint 2 do plano de melhorias, item 2.6 — Sentry + métricas mínimas.** Sentry opcional (`app/observability.py::configure_sentry`, ligado no `lifespan` de `app/main.py`): `SENTRY_DSN` vazia (default) = integração inteiramente desligada (`sentry_sdk.init()` nunca roda, `capture_exception()` vira no-op — confirmado isoladamente). `_unhandled_exception_handler` captura a exceção com `sentry_sdk.Scope().set_tag("request_id", ...).capture_exception(exc)` — `Scope()` **isolado por chamada** (não `configure_scope`/`push_scope` global), para não vazar a tag entre requests concorrentes no mesmo processo. Novo módulo `app/metrics.py` (contadores em memória por-processo, sem Prometheus — mesmo espírito do cache/rate-limit locais já existentes, ressalva idêntica de migrar pra Redis com `>1` réplica): `registrar_request()` chamado uma vez por request dentro do `RequestContextMiddleware`, mesmo ponto que já mede `duration_ms`. Novo `GET /metrics` (`app/routes/health.py`, mesmo gate de `X-Diagnostics-Token` de `/health/ready`/`/health/config`): `requests_total`/`requests_5xx_total` (torna o critério de go-live "48h sem 5xx" — Seção 6.3 — verificável sem grep de log), `latency_p95_ms_por_rota` (janela de até 500 amostras por rota), `polling_304` (taxa de acerto do ETag/304 da fila, Seção 2.2), `db_pool` (novo `app/db.py::pool_stats()` — saturação do pool asyncpg) e `sentry_enabled`. **Uptime check externo fica pendente** — decisão/ação do gestor (assinatura de serviço terceiro tipo UptimeRobot/Better Stack), fora do alcance de um PR; recomendação registrada no item 2.6 do plano de melhorias. Testes novos: `tests/test_metrics.py` (5 casos puros) + 6 casos em `tests/test_health.py`. Suíte completa (exceto `tests/e2e`) e `ruff app/` verdes; `npm run build:css` sem diff. |
| 2026-07-16 | 3.3 / 5.1 / Estado | **Sprint 2 do plano de melhorias, item 2.2/M2 (fase workspace) — camada de serviço `AtendimentoService`.** Novo pacote `app/services/` com `app/services/atendimento.py`: `AtendimentoService.dept_bate()` (setor do chamado bate com o setor do staff logado) e `AtendimentoService.permissoes()` (`eh_autor`/`eh_autoatendimento`/`bloqueado_por_autoria`/`pode_reivindicar`/`pode_atender`), classe stateless chamada direto pela rota. **Achado real da auditoria:** `app/templates/workspace/kanban.html` recomputava `dept_bate` no próprio Jinja com a mesma fórmula copiada à mão de `_carregar_atendimento` (`app/routes/workspace.py`) — as duas cópias já divergiam sutilmente (uma dependia de `perfil.departamento_id` truthy, a outra de `bool(...)` explícito). É a mesma classe de duplicação que originou o bug corrigido na migration `0028` (bypass de TI enxergando chamado de outro setor como atendível) — a RLS sempre bloqueou a escrita real, mas nada garantia que as duas cópias da regra de UI ficassem em sincronia. Corrigido: a rota `kanban()` agora anota `c["dept_bate"] = AtendimentoService.dept_bate(c, ctx.perfil)` em cada chamado antes de montar as colunas; o template só lê `c.dept_bate`, fonte única. `_carregar_atendimento()` passou a chamar `AtendimentoService.permissoes(chamado, ctx.perfil, ctx.user.id)` em vez de recalcular tudo inline — zero mudança de comportamento (mesma fórmula, só movida). Testes novos e puros em `tests/test_atendimento_service.py` (8 casos, incluindo o cenário específico do bug `0028`: setor diferente nunca "bate"); suíte completa (226 testes) e `ruff` verdes. **Escopo deixado para as próximas fases do mesmo item:** admin (dual-write de papel) e portal (abertura de chamado) ainda não migraram para a camada de serviço; os `dep.nome == 'Marketing'`/`chamado.departamento == 'Marketing'` remanescentes (rótulos de status, colunas extras do Kanban, formulário de meta do Marketing) são feature flag de exibição, não permissão — não dá para reaproveitar a coluna `autoatendimento` (migration 0042, cobre Marketing **e** RH) para isso, já que aquelas colunas/campos são Marketing-only (migrations 0024/0043); trocar por uma flag própria exige migration e decisão de produto dedicadas. |
| 2026-07-16 | Estado (Observabilidade) | **Sprint 2 do plano de melhorias, item 2.3/M5 — middlewares ASGI puros.** `SecurityHeadersMiddleware` (`app/security/headers.py`), `RequestContextMiddleware` (`app/observability.py`) e `SessionRefreshMiddleware` (`app/auth/session.py`) reescritos como callables ASGI puros (`__init__(self, app, ...)` + `async def __call__(self, scope, receive, send)`), em vez de `BaseHTTPMiddleware` — que reconstrói a resposta inteira numa task/stream separada a cada request. Cada um passa a interceptar só o evento `http.response.start` (via `MutableHeaders(scope=message)`) para injetar headers/cookies/request-id, sem tocar no corpo da resposta. `request_id` e `refreshed_session` (usado pelo refresh de sessão da Seção 3.4) migraram de `request.state.x = y` (que só funcionava dentro do `dispatch` do `BaseHTTPMiddleware`) para `scope["state"]` diretamente — mesmo dicionário que `Request.state` já lia, então nenhuma rota/dependency mudou. `app.add_middleware` em `app/main.py` não precisou de nenhuma alteração (aceita middleware ASGI puro do mesmo jeito); ordem da pilha inalterada (GZip → contexto → refresh de sessão → headers → router). **Benchmark:** `GET /workspace/fila/fragmento` via `TestClient` com dependências trocadas por fakes (sem banco), 500 requisições após 50 de aquecimento, mesmo processo/máquina comparando via `git stash` antes/depois — mean 5.29ms→4.32ms, p50 5.10ms→4.16ms, p95 6.04ms→5.12ms (~18% mais rápido nessa rota; o custo vinha do próprio `BaseHTTPMiddleware`, que roda cada camada com um stream intermediário). Suíte pytest completa (exceto `tests/e2e`, que exige Docker) verde. |
| 2026-07-15 | 0.2 / 2.5 / 2.9 / 3.2 / 3.9 / 7 / Estado | **Sprint 2 do plano de melhorias (itens 2.5/M8, 2.7/B4, 2.9/B1-B2-B3-B5) — itens rápidos e de baixo risco.** (a) **2.5 (M8) fonte única de deps:** `pyproject.toml [project.dependencies]` (puro espelho morto — sem `[build-system]`, nunca instalado por `pip`) removido; `requirements.txt`/`requirements-dev.txt` seguem sendo a única fonte real (é o que Dockerfile e CI instalam). `.github/dependabot.yml` novo (pip/npm/docker/github-actions, agrupado, mensal). (b) **2.7 (B4) higiene documental:** changelog de 24 entradas (2026-06-26→2026-07-15) extraído deste MD pra `docs/CHANGELOG.md` (este arquivo) — o plano mestre (Seção 7) passa a só linkar pra cá; matriz de permissões da Seção 3.2 **reescrita** no modelo vigente (`0020`/`0027`/`0028`/`0038`/`0042` — TI não tem mais tratamento especial de leitura/escrita, líder de setor é visão só-leitura, autoatendimento é exceção só em setores com a flag), a tabela antiga (modelo `0010`) estava desatualizada desde 2026-07-06 e chegou a induzir um bug real documentado no changelog de 2026-07-08; primeiro ADR do projeto criado em `docs/adr/` cobrindo as decisões arquiteturais "grandes" (RLS via `SET LOCAL`+claims, pooling Supavisor transaction mode, pivô pra sistema interno por departamento, SLA em horário comercial, Railway como alvo único de deploy, cache/rate-limit local-por-processo com gatilho de Redis). (c) **2.9 itens menores:** **B1** checklist de scale-out (Seção 2.5 nova) consolidando os gatilhos de migração pra Redis (cache 2.3 + rate limit 2.4) e o resize do pool asyncpg, hoje nenhum bloqueia produção (1 réplica); **B2** decisão do bucket público `avatares` registrada explicitamente (Seção 3.9.1, contraste com `chamados-anexos` privado) com gatilho de reavaliação se a sensibilidade mudar; **B3** `app/security/jwt.py` renomeado pra `app/security/jwt_verifier.py` (desfaz a auto-referência de nome com o pacote `jwt`/PyJWT que ele importa — 3 call sites atualizados: `app/auth/dependencies.py`, `app/main.py`, `tests/test_jwt.py`); **B5** `tests/test_db_rls_claims.py` novo — 9 casos parametrizados de claims adversariais (aspas simples/duplas, backslash, unicode, tentativa de fechar o literal SQL cedo) contra `app/db.py::_apply_rls_claims`, decodificando a SQL final pelas regras de literal do Postgres pra confirmar round-trip exato. Suíte completa verde (ignorando os testes de upload que travam por `libmagic` só no Windows local, causa raiz já documentada no item 0.4 do plano de melhorias). |
| 2026-07-15 | 4.2 / 4.3 / Estado | **Suíte e2e de RLS automatizada (Sprint 1 do plano de melhorias, item 1.7/M9) — a estratégia da Seção 4.2/4.3 (testes de integração contra Supabase local, RLS de verdade, impersonação via `SET LOCAL ROLE`+`set_config`, fixtures transacionais com rollback) sai do papel e vira suíte real.** `supabase/config.toml` criado (1ª vez que este repo ganha Supabase local — `db.seed`/`edge_runtime`/`analytics` desabilitados, não usados). `tests/e2e/conftest.py`: fixture `conn` abre uma transação por teste que nunca comita (rollback = limpeza, sem truncar tabela); fixture `seed` cria departamentos/usuários/chamados reais (mesmo padrão SQL do `docs/tutorial_usuarios.md`); helper `as_user()` **reusa `app.db._apply_rls_claims`** (código de produção, não uma reimplementação à parte) para trocar de persona. `tests/e2e/test_rls_matrix.py`: 10 testes cobrindo autor, staff RH/Marketing, líder de setor (`0028`, incl. prova de que é só leitura), TI pós-`0020`, autoatendimento Marketing/RH (`0038`/`0042`) vs. TI sem a flag (deve seguir bloqueado), nota interna, avatar 1º envio+reenvio (`0037`). **Realtime/`is_interna`:** sem teste WebSocket separado — documentado que o Realtime do Supabase aplica a MESMA policy `mensagens_select` na entrega de `postgres_changes`, então o SELECT direto já prova a mesma garantia. `.github/workflows/e2e-rls.yml`: job separado do CI principal, só dispara em paths `supabase/migrations/**`/`app/repositories/**`/`app/db.py`/`tests/e2e/**`. Marker `rls` registrado em `pyproject.toml`; sem `RLS_DATABASE_URL` a suíte é pulada automaticamente (não quebra o `pytest` default). ⚠️ **Sem Docker no ambiente desta sessão** — suíte escrita e revisada linha a linha contra as 45 migrations reais, mas não rodada de ponta a ponta contra Postgres local ainda; 1ª execução real será no CI ou localmente por quem tiver Docker. Suíte pytest principal (208 testes, sem os 11 novos que ficam skip sem Docker) verde. |
| 2026-07-13 | 5.1 / 3.9 (SLA) / C3 (build) | **Kanban do Marketing: filtros, rolagem por coluna, etiquetas sem cor em produção e pausa visual do SLA em "Aguardando terceiros".** Quatro pedidos do usuário sobre o Kanban recém-lançado (linha acima). (a) **Filtros:** `/workspace/kanban` chamava `ChamadosRepo.fila()` sem nenhum filtro; agora aceita período (`data_de`/`data_ate` sobre `created_at`), `setor`, `categoria`, `prioridade`, `operador` e `sla` — mesmo padrão de formulário GET autosubmit de `workspace/fila.html`. `ChamadosRepo.fila()` ganha os parâmetros `setor`/`data_de`/`data_ate`; novo `ChamadosRepo.setores_ativos()` alimenta o select. `shell.js`: autosubmit passa a valer também para `input[data-autosubmit]` (antes só `select`), usado pelos campos de data. (b) **Rolagem por coluna (estilo Trello):** `#kanban` ganha altura fixa (`h-[calc(100vh-16rem)]`) e `overflow-x-auto`; cada `.kanban-col` vira `flex-1 min-h-0 overflow-y-auto` — antes a coluna crescia sem limite e quem rolava era a página inteira, escondendo o quadro inteiro atrás de um scroll longo com muitos cartões. (c) **Etiquetas de setor/categoria sem cor em produção:** causa raiz não era o template — `cor_rotulo()`/`PALETA_ROTULOS` (`app/templating.py`) sempre estiveram corretos e o `app.css` local sempre teve as classes. O bug estava no `Dockerfile`: o estágio de build do CSS só copiava `app/static/src` e `app/templates` pro contexto do Tailwind CLI, mas `tailwind.config.js` também escaneia `app/**/*.py` (onde vive a paleta) e `app/static/js/**/*.js` — como esses arquivos não existiam no build stage, o purge de produção derrubava essas classes (o container servia um `app.css` sem elas, mesmo com o build local correto). Fix: `COPY app ./app` no estágio `css`, cobrindo os mesmos globs do `content` do Tailwind. (d) **Pausa do SLA:** `estado_sla`/`barra_sla` (`app/domain/sla_visual.py`) só checavam `resolvido_em` — um card em `AGUARDANDO`/`AGUARDANDO_TERCEIROS` continuava contando (e podia piscar "vencido") porque a trigger `sla_pausa_aguardando` (0044) só corrige `limite_resolucao` quando o chamado **sai** do status pausado, não enquanto está dentro. Novo parâmetro `status`: quando é `AGUARDANDO`/`AGUARDANDO_TERCEIROS`, retorna estado `"pausado"` (neutro, sem pulsar, "Prazo pausado") em vez de recalcular em cima do prazo ainda não corrigido — aplicado nos dois status (mesma trigger, mesma semântica), não só em "Aguardando terceiros". Chamadores atualizados: `sla_chip`/`sla_barra` (`workspace/_macros.html`), `_notificacoes.html`, filtro de SLA da fila (`_aplicar_sla` em `app/routes/workspace.py`). Testes novos em `test_sla_visual.py` (estado pausado mesmo com prazo tecnicamente vencido) e `test_workspace.py` (filtros do Kanban repassados ao repo, form repopulado, link "limpar filtros"); suíte completa (182 testes) verde; `npm run build:css` sem erros.** |
| 2026-07-13 | 5.1 / 3.9 (SLA) / Estado | **Nova coluna do Kanban do Marketing: "Aguardando terceiros" (migrations `0043`/`0044`), entre "Em andamento" e "Aguardando Validação".** Pedido do usuário: separar "esperando o solicitante validar" (`AGUARDANDO`, já existia) de "esperando um fornecedor/parceiro externo" (`AGUARDANDO_TERCEIROS`, novo) — hoje os dois caíam juntos em "Aguardando". Decisão confirmada com o usuário: o novo status **pausa o prazo de entrega**, mesma lógica do `AGUARDANDO` (0017) — não é o setor que está parado. (a) **`0043`:** `ALTER TYPE status_chamado ADD VALUE 'AGUARDANDO_TERCEIROS'`, isolado em migration própria (um valor de enum novo não pode ser usado na mesma transação em que foi criado — mesmo padrão da `0024`/`A_FAZER`). (b) **`0044`:** generaliza a trigger `sla_pausa_aguardando()` pra tratar `AGUARDANDO` e `AGUARDANDO_TERCEIROS` como "pausados" — transitar direto entre os dois mantém o relógio pausado sem resetar `aguardando_desde`. (c) **App:** `STATUS_VALIDOS`/`status_list` do Kanban (`app/routes/workspace.py`) ganham o novo status (só a coluna do Marketing o usa, mesmo padrão do `A_FAZER`); `STATUS_META` (`app/templating.py`) com rótulo "Aguardando terceiros" e cor própria (`st_terceiros`, laranja `#EA580C`, novo token no `tailwind.config.js`); `ChamadosRepo.fila_stats` passa a contar o novo status (evita o card do Kanban ficar sem número, gap que já existia hoje pro `A_FAZER`). CSS reconstruído (`npm run build:css`). Teste novo em `test_workspace.py` valida a ordem das colunas e o card na coluna certa; suíte completa verde. |
| 2026-07-10 | 3.3 / 5.1 / Estado | **Marketing vira exceção da segregação de função (0029) — autoatendimento liberado (migration `0038_marketing_autoatendimento`, aplicada em `iurlzlhbnoemkzgexcfk`).** Pedido do usuário: o Marketing não é suporte (alguém de fora abre, o setor atende) — é o próprio time que cria e gerencia as demandas, um quadro estilo Trello; a regra "autor nunca atende o próprio chamado" (validada pra todos os setores em 0029, um dia antes) não fazia sentido lá. (a) **RLS:** `chamados_update_staff` permite `operador_id = cliente_id` quando `departamentos.nome = 'Marketing'`; `mensagens_insert` permite o autor postar como staff (pública ou nota interna) no próprio chamado, mesma exceção — continua exigindo `operador_id IS NOT NULL` (alguém, inclusive o próprio autor via autoatendimento, precisa ter "iniciado o atendimento" antes). (b) **Repositório:** `ChamadosRepo.iniciar_atendimento`/`atribuir` buscam o nome do departamento e só bloqueiam autoatendimento fora do Marketing. (c) **Rota/template:** `pode_atender`/`pode_reivindicar` (`app/routes/workspace.py`) não excluem mais o autor quando o departamento é Marketing; `workspace/atendimento.html` para de redirecionar o autor pro Portal e mostra composer/Ações normalmente; dropdown de "Operador responsável" passa a listar o próprio autor no Marketing. Validado ao vivo contra o Supabase (simulação de claims do Felipe Schoffler, ADMIN de Marketing, em transação com `ROLLBACK`): autoatendimento (`UPDATE operador_id = cliente_id`) e mensagem de staff no próprio chamado, ambos antes bloqueados pela RLS, passaram a funcionar; TI/RH inalterados (a condição extra só é verdadeira pro Marketing). Suíte completa (72 testes) verde; advisors sem novidade. |
| 2026-07-10 | 5.1 / Estado | **Kanban/fila do Marketing param de esconder chamados próprios/de colega; foto de perfil no chat.** (a) **Kanban/fila = quadro do setor, não fila de suporte, no Marketing:** `ChamadosRepo.fila`/`fila_assinatura`/`fila_stats` excluíam chamados abertos pelo próprio usuário ou por colega do mesmo setor (pensado pro modelo TI/RH — "de fora do setor" é quem a fila atende); no Marketing isso escondia a maior parte das demandas do próprio quadro Trello do time. Fix: exceção `dep.nome = 'Marketing'` nas três queries — toda demanda do setor aparece no kanban/fila, igual já aparecia em "Meus chamados"; TI/RH sem mudança. Validado ao vivo (Supabase, simulação de claims do Felipe Schoffler): via 3 de 8 chamados do Marketing na fila, passou a ver os 8; TI (2→2) e RH (10→10) inalterados. (b) **Foto de perfil no chat:** `_mensagens.html` (compartilhado Portal/Workspace) sempre mostrava a bolinha com a inicial do nome, nunca a foto real — `ChamadosRepo.mensagens` não buscava `avatar_path` do remetente. Fix: query traz `remetente_avatar_path`/`remetente_avatar_atualizado_em`; template usa `avatar_url(...)` (mesmo helper da foto do autor), com fallback pra inicial se não houver avatar. Suíte `test_workspace.py` (30 testes) verde. |
| 2026-07-09 | 5.1 / Estado | **Reposicionamento da foto do autor (Workspace).** A bolinha de avatar de quem abriu o chamado (`avatar_url`, já implementada na Fase 7) passa a aparecer em dois lugares onde faltava/estava mal posicionada, a pedido do usuário: (a) **tela de atendimento** (`workspace/atendimento.html`) — antes só o texto "Autor: nome" aparecia no card; agora a foto (36×36, `ring-line`) fica ao lado desse bloco de info, à esquerda do texto (`obter()` já trazia `cliente_avatar_path`/`cliente_avatar_atualizado_em`, só faltava o `<img>` no template). (b) **Kanban** (`workspace/kanban.html`) — a foto (20×20) sai do canto superior absoluto do cartão (perto do botão de excluir) e passa a ficar inline ao lado do título. **Nota de build:** o projeto não tem Node/npm instalado nesta máquina para rebuild do Tailwind CLI (`app.css` é um build estático versionado) — as classes novas (`w-9`, `ring-line`, `gap-1.5`) foram escolhidas dentre as já compiladas no `app.css` existente (confirmado via grep no CSS minificado) em vez de classes arbitrárias (`w-11`/`ring-line/60` inicialmente tentadas não renderizavam — a foto aparecia sem recorte/tamanho). Validado ao vivo contra o Supabase (`iurlzlhbnoemkzgexcfk`): chamado `BOND-2026-00025` (autor Giordano Burtet, com avatar cadastrado) renderiza a foto corretamente na tela de atendimento. Suíte `test_workspace.py` (30 testes) verde — nenhum teste depende do markup exato do avatar. |
| 2026-07-09 | 5.1 / Estado | **"Meu perfil" sai da barra lateral pro menu do usuário; "Meus chamados" e "Chamados do Departamento" viram uma página só.** (a) **Perfil no menu do usuário:** o item "Meu perfil" saiu da navegação lateral (workspace e portal) e passou a viver no dropdown que abre ao clicar no avatar/nome no canto superior direito (mesmo dropdown que já tinha "Sair"), em `workspace_base.html` e `portal/app_base.html`. (b) **Unificação de "Meus chamados" + "Chamados do Departamento":** a rota/página separada `/workspace/departamento` (repo `chamados_departamento`, template `workspace/departamento.html`) foi removida; seu conteúdo virou uma seção a mais dentro de `portal/dashboard.html` (rota `GET /portal`, já usada por "Meus chamados"). A seção só é buscada/renderizada quando `perfil.role == 'ADMIN'` — i.e., só pro **líder de setor** (mesmo público que a RLS da `0028` já dava a visão extra); OPERADOR comum e funcionário (CLIENTE) veem só a própria lista, como antes. Item de menu "Chamados do Departamento" removido da barra lateral. `ChamadosRepo.chamados_departamento` (já corrigido nesta sessão, ver linha acima) é reaproveitado sem mudança de assinatura. Testes: `tests/test_portal.py` ganhou `_admin()` + `FakeRepo(role=..., chamados_colegas=...)` e dois testes novos (seção aparece pro líder, some pro funcionário); `tests/test_workspace.py` teve os dois testes da antiga aba trocados por checagens de que o link sumiu e que "Meu perfil" está no dropdown. Suíte completa (219 testes) verde. |
| 2026-07-09 | 5.1 / Estado | **Fix: "Chamados do Departamento" escondia chamados de colega destinados a outro setor (`app/repositories/chamados.py::chamados_departamento`).** A divisão em três recortes da própria Fase 1 (linha abaixo) exigia `c.departamento_id = meu_setor` (destino) **além** de "autor é colega" — mas a RLS de líder de setor (`0028`, dia anterior) já concede leitura de qualquer chamado cujo **autor** seja do meu setor, **independente do destino** (ex.: um colega de TI abre "Solicitação de férias"/"Desligamento" para o RH). Resultado: o sino de notificações (sem filtro extra, só RLS) mostrava 3 chamados de um colega, mas a aba só listava 1 (o único também destinado ao TI). **Fix:** a query passa a filtrar só por `autor.departamento_id = meu_setor`, largando a exigência de destino — RLS continua sendo quem decide o que cada papel realmente enxerga (líder ADMIN ganha as linhas extra; OPERADOR comum não, pois a policy `0028` só libera esse ramo pra ADMIN). Confirmado ao vivo contra o Supabase (`iurlzlhbnoemkzgexcfk`): consulta simulando os claims de um ADMIN de TI, em transação com `ROLLBACK`, passou de 1 para as 4 linhas esperadas (3 no RH + 1 no TI). Suíte `test_workspace.py` (mocks, não exercita a RLS real) segue verde sem alteração — a divergência só aparece contra o banco. |
| 2026-07-09 | 3.8 / 5.1 / Estado | **3º fix da foto de perfil — CSP bloqueava a própria imagem (`app/security/headers.py`).** Depois dos 2 fixes anteriores (linhas abaixo) o upload passou a funcionar de ponta a ponta (RLS + Storage OK), mas a foto aparecia como ícone de imagem quebrada no navegador. Causa: `img-src` da CSP (Seção 3.8) é `'self' data:` — nunca liberou o host do Supabase. Isso nunca tinha aparecido porque **anexos** (a única outra feature que referencia o Storage no HTML) são **links de download** (signed URL num `<a href>`), nunca um `<img src>` embutido; a foto de perfil é o **1º caso** de imagem cross-origin renderizada inline (`<img src="{{ avatar_url(...) }}">` no sidebar dos 3 shells + cards de fila/kanban/detalhe + `/perfil`) — o navegador bloqueia silenciosamente por CSP (sem erro visível pro usuário além do ícone quebrado; o servidor nem vê a requisição, é bloqueio client-side). `connect-src` já liberava o host do Supabase (pro `supabase-js` do chat/realtime) mas isso não cobre `<img>`, que é regido por `img-src`. Fix: `img-src` passa a incluir `settings.supabase_url` (mesmo host já confiável do `connect-src`) quando configurado — o bucket é público, então não há exposição de segredo. |
| 2026-07-09 | 3.3 / 5.1 / Estado | **2º fix da foto de perfil — causa raiz real era RLS, não a aplicação (migration `0037`, aplicada em `iurlzlhbnoemkzgexcfk`).** O fix anterior (linha abaixo) resolvia um travamento real, mas o usuário testou ao vivo e o reenvio da foto continuou falhando ("Falha ao enviar a foto"). Investigação ao vivo (mesma técnica de simulação de claims da Seção 4.2, em transação com `ROLLBACK`) achou a causa: a `0035` removeu `avatares_select` achando redundante pra um bucket público (verdade pra **leitura** pública, que não passa por RLS) — mas o path do avatar é **fixo por usuário** (`{user_id}/avatar.png`, `x-upsert: true`) justamente pra reenvio **substituir** a foto anterior, e reenviar um objeto que já existe roda como **UPDATE** por baixo do Storage. Postgres só deixa um role dar UPDATE/DELETE em linhas que ele também conseguiria enxergar via SELECT — **sem nenhuma policy de SELECT, `authenticated` não via nenhuma linha do bucket, nem a própria**, então o UPDATE afetava 0 linhas silenciosamente e o Storage devolvia erro. Resultado prático: **o 1º upload sempre funcionava** (é um INSERT, não precisa de SELECT) **e todo reenvio subsequente falhava** — exatamente o sintoma relatado. Confirmado ao vivo antes e depois do fix (`UPDATE ... WHERE bucket_id='avatares' AND name='<uid>/avatar.png'` pro próprio dono: 0 linhas sem a policy, 1 linha com ela). **Fix:** nova policy `avatares_select_own` — `FOR SELECT USING (bucket_id = 'avatares' AND (storage.foldername(name))[1] = auth.uid()::text)`, restrita ao **próprio** avatar (não reabre a listagem geral de todos os avatares que a `0035` removeu com razão). `get_advisors` sem novos achados após o fix. |
| 2026-07-09 | 3.9 / 5.1 / Estado | **Fix da foto de perfil (Fase 7) — causa raiz do upload "impossível" + recorte automático em quadrado.** (a) **Causa raiz encontrada e corrigida:** `POST /perfil` travava **indefinidamente** (nunca respondia) em dev Windows — reproduzido com `faulthandler`: `app/security/uploads.py::detectar_mime` fazia `import magic` sem guarda, e o `python-magic-bin` (pacote só-Windows do `requirements.txt`, workaround para dev sem libmagic real) trava dentro do próprio `import` (`mime_magic.load()`), não levanta exceção — o `try/except` existente não protegia contra isso. Corrigido com uma sonda **de uma vez só por processo**, rodada numa thread daemon com timeout de 2s (`_magic_disponivel_probe`): se não voltar a tempo, o processo trata `magic` como indisponível dali em diante e cai no sniffer de assinatura (`sniff_signature`, já cobre jpg/png/pdf/zip/mp4) — sem travar nenhum request depois. Não é um problema exclusivo do avatar: **qualquer** upload (anexos inclusive) passava pelo mesmo `detectar_mime` e sofria o mesmo travamento em dev Windows; a produção (Linux/Docker, libmagic real, sem `-bin`) não é afetada. (b) **Recorte automático em quadrado:** nova `app/avatar_storage.py::preparar_avatar` (Pillow, dependência nova — `Pillow==12.3.0` em `requirements.txt`/`pyproject.toml`) reusa `validar_anexo` e, se a imagem passar, aplica `ImageOps.exif_transpose` (corrige fotos giradas por metadado EXIF), recorta um quadrado centralizado (`min(largura, altura)`) e redimensiona para **512×512**, sempre normalizando a saída para **PNG** — independente do formato/proporção enviados (JPG retangular, por exemplo). `/perfil` e o upload de avatar na criação de conta (`/admin/usuarios`) passam a gravar sempre `{user_id}/avatar.png` (antes preservava a extensão original). O recorte no arquivo persistido é redundante com o `rounded-full object-cover` do CSS (que já exibe qualquer proporção como bolinha), mas garante o mesmo resultado em qualquer contexto que sirva a URL pública fora da página (ex.: cliente de e-mail) e evita guardar a foto original em resolução/proporção arbitrária no bucket público. (c) **Testes:** fixture de `tests/test_perfil.py` trocada de um PNG 1×1 com bytes inválidos (passava no sniffer por assinatura, mas o Pillow rejeitava por não ser um PNG decodificável de verdade — teria mascarado o bug) para um PNG **3×5 real** gerado pelo próprio Pillow; nova asserção decodifica o PNG salvo e confirma `largura == altura`. Suíte completa **170 testes** verde (rodada duas vezes: venv throwaway e o `.venv` real do projeto, ambas com `python-magic-bin` instalado — sem travar). |
| 2026-07-08 | 3.2 / 3.3 / 5.1 / 6 (Fase 4/5) / Estado | **Setor unificado com Departamento + "líder de setor" (migrations `0027`/`0028`, aplicadas em `iurlzlhbnoemkzgexcfk`) + fix de um bug introduzido nesta mesma sessão.** (a) **`0027` — unificação:** "Setor" (afiliação de quem abre — antes lista Python hardcoded `SETORES` em `app/routes/portal.py`, duplicada da tabela `departamentos`) passa a ser a **mesma tabela** `departamentos`, com a nova coluna `recebe_chamados` (`false` por padrão) marcando quais setores têm fila/staff e por isso podem ser **destino** de chamado (hoje só TI/RH/Marketing); os demais 7 setores (Comercial, Financeiro, Controladoria, Diretoria, Dpto Químico, SIG, Brigadistas) só identificam quem abriu. Trigger `enforce_departamento_recebe_chamados` barra roteamento de chamado pra um setor sem fila. `ChamadosRepo.departamentos_ativos` passa a trazer `recebe_chamados`; novo `departamentos_destino_ativos` filtra só quem recebe (usado no dropdown de abertura e no repasse do Workspace). Admin `/admin/gestao` ganha toggle "recebe chamados" por setor. (b) **`0028` — líder de setor:** todo cadastro em `/admin/usuarios` passa a exigir um `departamento_id` — inclusive **Funcionário/CLIENTE** (antes só staff tinha; `OPERADOR` continua restrito a setor com fila, `_depto_perfil_valido`). Isso viabiliza o **líder**: um `ADMIN` com `departamento_id`, mesmo num setor **sem fila** (ex.: gestor do Comercial), agora **vê** (RLS `chamados_select`/`mensagens_select`/`historico_select`) todo chamado cujo **autor** pertence ao mesmo setor — mesmo que o destino seja outro departamento. É só leitura: quem **atende** continua sendo exclusivamente o staff do departamento de **destino**; `workspace/atendimento.html` esconde as ações (iniciar, encerrar, mudar status/prioridade, responder, excluir, marketing-meta) quando `chamado.departamento_id != perfil.departamento_id`, mostrando um aviso de "só acompanhando". Guarda-corpo antigo em `perfis.departamento_id` (exigia `recebe_chamados`) foi **removido** — não fazia mais sentido com todo usuário tendo setor. (c) **Bug introduzido e corrigido na mesma sessão:** a 1ª versão do gate de atendimento dava um bypass incondicional pro TI (`pode_atender = is_ti OR mesmo_departamento`) — mas a **`0020`** (nunca documentada neste MD até agora, ver bloco 🔁 na Seção 3.2) já tinha **revogado** o "TI vê/atende tudo" em 2026-07-06; TI hoje só atende o próprio setor, ganhando um chamado alheio só via **repasse**. Isso deixava o botão "Iniciar atendimento" visível pro TI em chamados de outros setores; verificado ao vivo que a RLS (`chamados_update_staff`, inalterada) bloqueou a escrita de fato (0 linhas afetadas, status seguiu `NOVO`) — sem corrupção de dado, só UX enganosa. Corrigido removendo o bypass (`pode_atender = mesmo_departamento`, TI incluído). Suíte local (venv throwaway, sem alterar o ambiente do usuário) **86 testes** verde (`test_portal`/`test_workspace`/`test_admin`; novos: OPERADOR barrado em setor sem fila, líder ADMIN liberado em setor sem fila, view-only na tela de atendimento). ⚠️ Kanban (`/workspace/kanban`) ainda permite tentar arrastar/excluir um cartão fora do próprio setor — a RLS bloqueia a escrita real, mas a UI não impede a tentativa (rough edge sinalizado ao usuário, não corrigido nesta sessão). |
| 2026-07-08 | 5.1 / Estado | **Kanban: colunas sem corte de tela + data da solicitação no cartão; catálogo do TI reconstruído.** (a) **Colunas do Kanban sempre visíveis:** o container trocou `overflow-x-auto` + `min-w-[280px] shrink-0` por colunas `flex-1 min-w-0` — as 4-5 colunas (RH/TI ou Marketing) dividem a largura disponível e encolhem juntas em vez de forçar rolagem horizontal que cortava a última coluna (`Concluídos`/`Resolvido`) fora da tela. (b) **Data da solicitação no cartão:** badge "📅 Solicitado em dd/mm/aaaa" (mesmo padrão visual do chip de prazo `sla_chip`) acima da linha de prioridade/SLA restante — antes só aparecia no detalhe do chamado, não no Kanban. (c) **Catálogo de categorias do TI reconstruído** a partir do documento de referência `Categorias_Portal_Suporte.docx` (análise de 445 chamados reais): migration `0026_categorias_ti_docx` substitui as 15 categorias/37 subcategorias do seed `0018` por **12 categorias / 62 subcategorias** (o texto do documento resume "52", mas a lista enumerada tem 62 itens — a migration segue a lista). Aplicada em `iurlzlhbnoemkzgexcfk`; nenhum chamado referenciava o catálogo antigo do TI, então a troca foi direta (sem perda de histórico). |
| 2026-07-08 | 3.2 / 3.9 / 5.1 / 6.3 / Estado | **Correções pontuais (validação local, sem push): exclusão de chamado, link de e-mail quebrado, CSV do relatório e anexo de imagem por e-mail.** (a) **Exclusão de chamado errado** (operador/admin): migration `0025_chamados_delete_staff` — policy `DELETE` na `chamados` no mesmo escopo de `chamados_update_staff` (TI qualquer um; RH/Marketing só o próprio setor; funcionário **nunca**); `mensagens`/`historico_chamados` somem via `ON DELETE CASCADE` já existente. `ChamadosRepo.excluir`, rota `POST /workspace/chamados/{id}/excluir`. UI: ícone de lixeira vermelha em cada cartão do Kanban (confirmação via `confirm()` em `workspace.js`, CSP-safe) e zona de exclusão com confirmação em duas etapas na tela de atendimento (mesmo padrão de `admin/usuarios.html`). (b) **Fix do botão "Visualizar e Responder" do e-mail de notificação (404):** `notification.py` montava a URL com `codigo` (`BOND-YYYY-NNNNN`) em rotas que não existem (`/chamado/{codigo}`, `/workspace/atendimento?codigo=`); as rotas reais resolvem por **`id` (uuid)** — `/portal/chamados/{id}` (funcionário) e `/workspace/chamados/{id}` (staff), que é o que `detalhe_chamado`/`obter` esperam (`WHERE c.id = $1::uuid`). (c) **Export CSV** (`GET /admin/export/csv`): sem BOM UTF-8 o Excel pt-BR exibia acentos como mojibake (`ApresentaÃ§Ã£o`); adicionado `﻿` no início do conteúdo, delimitador `;` (padrão do Excel pt-BR, evita colisão com o separador decimal `,`) e cabeçalho traduzido/legível (`Chamado`, `Título`, `Data de criação`, …) em vez das chaves internas do banco; datas formatadas em `dd/mm/aaaa HH:MM` (horário de Brasília, reaproveitando `fmt_dt`) em vez do ISO cru com offset. (d) **Anexo de imagem via resposta por e-mail:** o webhook `/api/inbound-email` sempre gravava `anexos: '[]'` — uma imagem anexada na resposta era descartada (só o texto, às vezes com um link/cid solto da assinatura, ia pro chat). Agora extrai `attachment-1..N`/`attachment-count` do payload do Mailgun, valida (mesmas regras de `app/security/uploads.py` — 10MB, allow-list, magic bytes) e envia ao Storage privado **com a `service_role` key** (única exceção ao padrão "token do usuário" da Seção 1.4/3.9: rota de webhook server-to-server, sem cookie de sessão; uso pontual e já gated pela verificação HMAC do token de resposta). Mensagem só-imagem (sem texto) deixa de ser rejeitada como "conteúdo vazio". Testes: `test_workspace.py` (lixeira no Kanban + fluxo de exclusão em 2 etapas + redirecionos por origem), `test_admin.py::test_export_csv` repontado p/ BOM+`;`+cabeçalho pt-BR, `test_inbound_email.py` com `empresa_id` no mock do chamado. ⚠️ Sessão de validação local — **sem commit/push**, a pedido do usuário. |
| 2026-07-05 | 5.2 / 6 (Fase 5) / Estado | **Ajustes solicitados + SLA em horário comercial.** (a) **SLA comercial (migration `0017_sla_horario_comercial`, aplicada em `iurlzlhbnoemkzgexcfk`):** decisão do gestor — expediente **seg–sex 08:00–18:00 (America/Sao_Paulo)**, para em **feriados** (tabela `feriados`, RLS: leitura por autenticado, escrita só TI; seed nacional 2026–2028, editável) e o status **AGUARDANDO pausa** o relógio de resolução. Funções `sla_minutos_uteis_entre`/`sla_add_minutos_uteis` (SECURITY DEFINER, EXECUTE revogado), trigger `sla_pausa_aguardando` (BEFORE UPDATE OF status, coluna `chamados.aguardando_desde`) e `calcular_sla_chamado` recompilada para minutos úteis. **Substitui as horas corridas da decisão C1.** Verificado ao vivo (virada de dia/fim de semana/feriado + pausa 120min empurra o prazo; testes revertidos). ⚠️ `app/domain/sla_visual.py` (barra) segue em tempo de parede — a cor/atraso por prazo absoluto continua correta; barra de progresso é aproximação. (b) **Seletor de setor no `/admin`:** o chip "Indicadores de:" virou **dropdown** (TI escolhe RH/Marketing/TI ou "Todos os setores"); KPIs/gráficos/avaliações refiltram por `departamento_id`. (c) **Edição de Planos de SLA** (`/admin/gestao` → `POST /admin/planos/{id}`, só TI): tempos por prioridade+default editáveis. (d) **Gestão de contas** (`/admin/usuarios`, só TI): criar colaborador (GoTrue Admin API via service_role) já confirmado + definir setor/papel, e alterar papel/setor de existentes — **dual-write** perfis+`app_metadata.role`. `app/auth/supabase_client.py::ensure_admin_client`. (e) **Anexos nas conversas do workspace** (staff): `responder` aceita multipart, `responder_staff` persiste anexos, fragmentos assinam URLs (antes staff via "indisponível"); helpers de upload extraídos p/ `app/anexos.py` (compartilhado portal+workspace). (f) **Fix RH:** link "Abrir chamado" restaurado no shell do workspace. (g) **Contas admin de setor** criadas p/ teste: `admin.rh@`/`admin.marketing@` (ADMIN, senha provisória). Suíte **107 testes** verde. |
| 2026-07-04 | 3.2 / 6 (Fases 4–5) / 6.1 / Estado | **Fases 4 (papéis & tempo real) e 5 (polimento & validação).** **(Fase 4)** **(a) Admin de departamento:** o painel `/admin` deixa de ser exclusivo do TI — agora entra também o **ADMIN de setor** (role `ADMIN` + `departamento_id`, ex.: gestor do RH), que vê os indicadores **escopados ao seu departamento** (CSAT, conformidade de SLA, rapidez, produtividade e as avaliações dos funcionários) — a RLS já escopa as queries. Gestão de catálogos permanece **só do TI** (`_require_ti`). `admin_context` resolve acesso+escopo por `perfil` (TI/ADMIN-setor/senão 403); OPERADOR/CLIENTE barrados. Sidebar do workspace mostra "Relatórios do setor" p/ ADMIN e "Admin (TI)" p/ TI; `admin_base` reflete o papel. Distinção OPERADOR×ADMIN documentada em `registro_usuarios.sql` (+ exemplo de gestor RH). **(b) Sino em tempo real:** `notificacoes.js` busca `/realtime/config` (JWT do próprio usuário, lido do cookie no servidor) e assina `postgres_changes` de `chamados`+`mensagens` (RLS na entrega) → acende/toca o sino e recarrega a lista aberta a cada mudança significativa (novo chamado, status/prioridade/atribuição, nova mensagem). Migration **`0016_realtime_chamados`** (aplicada em `iurlzlhbnoemkzgexcfk`): `chamados` adicionado à publicação `supabase_realtime` (agora `chamados, mensagens`). supabase-js + `notificacoes.js` carregados nos 3 shells; supabase-js duplicado removido do detalhe/atendimento. **(c) Chat Realtime verificado** (`chat.js` + publicação `mensagens` OK). **(d) Fluxo departamento→visibilidade validado ao vivo** (simulação de claims): RH (Rita) vê só os 7 chamados de RH, nunca Marketing/TI. **(Fase 5)** **(e) Redefinição de senha por OTP:** rotas `/esqueci-senha` (envia código via `reset_password_for_email`) e `/redefinir-senha` (`verify_otp` type recovery + `update_user`), cliente Supabase **isolado** por request (não compete com o global), rate limit 5/min, link no login. ⚠️ requer template de e-mail "Reset Password" com `{{ .Token }}`. **(f) Páginas 403/404** estilizadas (`erro.html`) via handler central (registrado na `StarletteHTTPException` p/ capturar 404 de rota inexistente); só em navegação HTML (HTMX/API seguem JSON). **(g) Animações fluidas:** `app/static/css/anim.css` (entrada de página/cartões, stagger, sino, barra de SLA, `prefers-reduced-motion`) — CSS estático sob CSP `style-src 'self'`, sem depender do build do Tailwind. **(h) Teste de carga:** `tests/load/locustfile.py` (100 CCU, rotas autenticadas, taxa de 304 no polling) + `tests/load/smoke_carga.py` (rajada bruta de concorrência com percentis). **(i) Suíte:** `tests/test_fase4_fase5.py` (sino, realtime config, reset de senha, 403/404) + `test_admin.py` repontado p/ o novo `admin_context` (operador 403, admin de setor vê dashboard mas não gere catálogos). ⚠️ pytest a rodar via `iniciar-dashboard.bat`/CI (máquina sem Python). |
| 2026-07-04 | 5.2 / 6 (Fase 4) / Estado | **Fase 3 (atendimento) — iniciar atendimento, barra de SLA, escopo de responsável e repasse por TI.** **Sem migration** (a RLS existente já cobre tudo). (a) **Iniciar atendimento:** botão que move `NOVO→EM_ATENDIMENTO` e assume o chamado como responsável (`repo.iniciar_atendimento`, idempotente; grava `ATENDIMENTO_INICIADO`); rota `POST /workspace/chamados/{id}/iniciar`; botão só aparece em `NOVO`. (b) **Barra de progresso de SLA** (`app/domain/sla_visual.py::barra_sla`, pura/testada): enche conforme o tempo passa sobre a janela `created_at→limite_resolucao` (**12h/24h por prioridade**, decisão C1 — sem os 4h do briefing); **verde** no início → **amarela a partir da metade** (≤50% restante) → **vermelha** <10% ou vencida (piscante). Largura aplicada por **CSSOM** (`shell.js` lê `data-sla-pct`) porque `style=""` inline é bloqueado pela CSP `style-src 'self'`; cores reutilizam `bg-sla-*` já compiladas. Macro `sla_barra` no atendimento. (c) **Responsável só do setor:** `repo.operadores(departamento_id=…)` filtra o staff pelo **departamento do chamado** (RH atribui RH; TI, ao atender um chamado de RH, atribui RH). (d) **Repasse por TI:** `repo.transferir` troca `chamados.departamento_id` (limpa o operador, grava `DEPARTAMENTO_ALTERADO`); rota `POST /workspace/chamados/{id}/transferir` com gate de UI **e** de rota por `perfil.is_ti` — e a RLS `chamados_update_staff` já **só** deixa o TI (`auth_is_ti()` no WITH CHECK) gravar um `departamento_id` fora do próprio setor. Selects usam `data-autosubmit` (sem botão "OK"). (e) **Testes:** `test_sla_visual` (barra: início/metade/quase-estouro/vencida/resolvida), `test_workspace` (iniciar visível/oculto/assume, operadores escopados, TI vê/repassa, não-TI não vê/não repassa). Rodados verdes via venv (`iniciar-dashboard.bat` agora instala Python). |
| 2026-07-03 | 5.1 / 5.3 / 3.9 / Estado | **Fase 2 (abertura de chamado) — subcategorias, anexos na abertura e campos obrigatórios.** (a) **Migration `0015_subcategorias`** (aplicada no projeto `iurlzlhbnoemkzgexcfk`): nova tabela `subcategorias` (catálogo global gerido pelo TI, `auth_is_ti()`; RLS `select=true`/`admin=auth_is_ti`), coluna `chamados.subcategoria_id` (FK `ON DELETE RESTRICT`, **NULLABLE** no banco p/ preservar chamados antigos; **obrigatória na rota**), índices e **seed** (Acessos 5 / Dúvida 3 / Equipamento 5 / Financeiro 4). Trava de coluna do CLIENTE (`enforce_cliente_so_avaliacao`) **estendida** com `subcategoria_id` (autor não reclassifica). Advisors sem novos erros. (b) **Backend:** `ChamadosRepo.subcategorias_ativas` (cache por categoria) + `subcategoria_valida` (defesa em profundidade contra par categoria/subcategoria forjado); `criar` persiste `subcategoria_id`; `obter` traz o nome da subcategoria. Rota `GET /portal/chamados/subcategorias` (cascade HTMX, declarada antes de `/{chamado_id}`); `POST /portal/chamados` agora exige **departamento+categoria+subcategoria+assunto+descrição** e aceita **anexos multipart na abertura** (validados ANTES de criar; viram a 1ª mensagem pública). Upload helper dividido em `_validar_uploads` (sem efeito colateral) + `_enviar_uploads` (Storage). (c) **UI:** form de abertura com `enctype=multipart`, select de subcategoria via cascade, input de arquivos (allow-list), estado preservado no erro; detalhe mostra `categoria › subcategoria`. (d) **Testes:** `tests/test_portal.py` estendido (subcategoria obrigatória, par inválido, cascade, anexo de tipo inválido barra a criação). ⚠️ **pytest não executado nesta sessão** (máquina sem Python) — rodar via `iniciar-dashboard.bat`/CI. |
| 2026-07-03 | 5.1 / 5.3 / 2.x / Estado | **Notificações, encerramento didático e revisão do schema.** (a) **Sino de notificações** na topbar (3 shells): rota única `GET /notificacoes` escopada por RLS (funcionário vê os próprios, staff o setor, TI tudo), dropdown carregado por HTMX + `shell.js`. (b) **Encerrar chamado** (workspace, staff): card em destaque com **nota de solução** — vira mensagem pública ao solicitante e marca `RESOLVIDO` numa só transação (`POST /workspace/chamados/{id}/encerrar`). Validado e2e. (c) **Revisão/refatoração do schema (banco):** as 9 tabelas canônicas estão **todas conectadas** (13 FKs), **sem tabelas sobressalentes nem faltantes**, integridade perfeita (0 órfãos/nulos). Migrations `0013` (índices de cobertura p/ FKs `chamados.cliente_id/categoria_id`, `mensagens.remetente_id`, `historico.ator_id` + `chamados.departamento_id` **NOT NULL**) e `0014` (**otimização RLS `auth_rls_initplan`**: `auth.*()`/helpers envolvidos em `(SELECT …)` → avaliados 1×/query em vez de por linha; isolamento **revalidado e2e**). Advisors restantes: só INFO de índices "não usados" (base pequena — mantê-los p/ produção) e helpers SECURITY DEFINER em `public` (backlog: schema privado). Suíte pytest **68 verde**. |
| 2026-07-03 | 2.1 / 2.2 / 2.3 / 2.4 / 3.4 / 3.5 / 5.1 / 6 / Estado | **Revisão geral, endurecimento e validação e2e ao vivo.** (a) **Deploy/serverless (2.1/2.2):** pool asyncpg efêmero na Vercel (`min_size=0`, `max_size≤2`, reciclagem rápida via `is_serverless`); `vercel.json` declarativo (`@vercel/static` p/ `/static`, `@vercel/python` p/ a app). (b) **Segurança:** migration `0012_harden_ti_admin_axis` (gestão de `perfis`/`empresas` movida de `auth_role()='ADMIN'` → `auth_is_ti()`, fechando escalação latente); **isolamento RLS validado e2e ao vivo** (RH só vê RH, TI vê tudo, autor não recebe nota interna). **Achado corrigido:** o gate do app lia o papel de `app_metadata.role` do JWT, mas a promoção só atualizava `perfis` → `registro_usuarios.sql` agora faz **dual-write** (perfis + `raw_app_meta_data`). (c) **Robustez:** rate limit na abertura de chamado (`app/ratelimit.py`, 15/min); **refresh de sessão** (renova access token via refresh + `SessionRefreshMiddleware`; 401 de navegação → `/login`); **ETag/304** no polling da fila; **cache TTL** de catálogos (`app/cache.py`, invalidado na escrita). (d) **Performance:** **uma conexão RLS por request** (holder lazy via contextvar), `SET LOCAL ROLE`+`set_config` num round-trip, **GZip**, dashboard sem query extra → `/portal` 1939ms→1278ms (piso ~320ms/query = latência ao banco remoto us-east-2; local fluido exige DB local). (e) **UI:** shell responsivo (sidebar off-canvas + hambúrguer + dropdown de usuário, `shell.js` vanilla CSP-safe), login 2 colunas, hero + cards no dashboard, SRI de `chart.umd.js`/`sortable.min.js`/`alpine-csp.min.js` **corrigidos** (bloqueavam gráficos/DnD); "Admin (TI)" só p/ TI e "Abrir chamado" removido do staff. (f) **Avaliação/relatórios (5.1):** caixa de comentário opcional já existia; **comentário agora vai aos relatórios do TI** — coluna `avaliacao_comentario` no CSV + painel "Últimas avaliações" no dashboard. (g) **Stack real:** sem Docker local, roda contra o Supabase de nuvem `iurlzlhbnoemkzgexcfk` (3 usuários de teste TI/RH/CLIENTE com perfis+JWT sincronizados, 5 chamados/mensagens semeados, bucket `chamados-anexos` criado). **Fluxo e2e validado ao vivo:** funcionário abre → acompanha → RH atende (chat público + nota interna) → pausa/resolve → autor avalia c/ comentário → reflete no dashboard/CSV do TI. Suíte pytest **68 testes** verde. |
| 2026-07-01 | 6 (Fase 5) / 6.2 / Deploy / Estado | **Fase 5 — Painel Admin & Relatórios** + **auditoria de segurança** + **preparação de deploy na Vercel.** (a) `/admin` restrito ao **TI** (`auth_is_ti()`): KPIs (conformidade de SLA, CSAT, TMA, produtividade), gráficos **Chart.js** (self-host+SRI), gestão de departamentos/categorias, planos (leitura) e **export CSV** (`GET /admin/export/csv`). (b) **Auditoria de segurança (6.2):** varredura de todo o repositório **e do histórico do git** — **nenhuma** chave/JWT/senha/token vazado; `.env` nunca commitado; `service_role` só em `config.py` (nunca em rota/template); ao browser vão só `SUPABASE_URL`+anon key (pública)+JWT do próprio usuário. (c) **Deploy Vercel:** `pyproject [tool.vercel]`, `vercel.json`, `.vercelignore`, init **lazy/idempotente** (pool/CSRF/JWT/Supabase/Storage) para runtimes sem lifespan, `app.css` versionado como fallback. `⚠️` a stack (servidor persistente + asyncpg + libmagic) é melhor servida no **Railway** (Dockerfile pronto). Suíte pytest **68 testes** verde. |
| 2026-07-01 | 6 (Fase 4) / 5.3 / Estado | **Fase 4 — Workspace do Operador.** Rotas `/workspace` (staff, `require_role` OPERADOR/ADMIN; escopo real por RLS `0010`): **Fila Lista** (contadores por status, filtro, polling HTMX 15s) e **Kanban** (Sortable.js DnD → persiste status via fetch+CSRF; fallback pelas ações da tela). **Indicador de SLA por cor** — `app/domain/sla_visual.py` (puro/testado): verde / amarelo <25% / vermelho <10% ou vencido (piscante). **Tela de atendimento**: ações rápidas de **status/prioridade/atribuição** (cada uma grava `historico_chamados`; prioridade recalcula SLA via trigger; `respondido_em` na 1ª resposta pública), chat Realtime reusado, e **composer com toggle "Nota interna"** (fundo amarelo via `workspace.js`; `is_interna` decidido no servidor). Vendorizado Sortable.js 1.15.6 (SRI). Login de ADMIN/OPERADOR passa a ir para `/workspace`. Suíte pytest **63 testes** verde (novos: SLA visual + rotas do workspace). |
| 2026-06-26 | Todas | Criação do plano mestre a partir da spec v2.0. Contradições C1–C3 resolvidas; decisões de RLS×pooling, cache tenant-scoped, CSRF/CSP/headers, Storage, schema canônico das 7 tabelas, código BOND seguro sob concorrência, topologia Realtime e cronograma com DoD por fase. |
| 2026-06-26 | 5.1 / 5.4 / 6.2 / Estado | Início da produção (Fase 1). Aplicadas migrations `0001_init_enums` (3 enums) e `0002_tables_core` (7 tabelas + índices) no projeto Supabase `iurlzlhbnoemkzgexcfk`, materializando o schema canônico da Seção 5.1; `.sql` versionados em `supabase/migrations/`. Adicionada **regra dura de segurança** (6.2): chaves de API nunca expostas em código, docs ou qualquer artefato — só em env vars. RLS ainda **desabilitado** nas 7 tabelas (trabalho da Fase 2). |
| 2026-06-26 | 0.2 / 1 / 2 / 3 / 6 / Estado | Fundação do backend (Fase 1 + Fase 2 backend). Scaffolding FastAPI (`app/`), `requirements.txt` com versões fixadas, Dockerfile multi-stage (Tailwind CLI + Python 3.12 slim + libmagic, porta 8080), Pydantic Settings, **camada de dados `asyncpg` com `SET LOCAL ROLE authenticated` + claims por transação** (Seção 3.1), cliente Supabase async só para Auth, **verificação local de JWT** (PyJWT, JWKS+HS256), **CSRF double-submit**, **security headers + CSP estrita**, cookies de sessão, rate limiting (slowapi em /login e /cadastro), logging JSON com request-id, `/health` + `/health/ready`, rotas `/login` `/logout` `/cadastro` e templates base. Suíte pytest (9 testes: health, CSP, CSRF, JWT) verde; build do Tailwind validado. Adicionado PyJWT à Seção 0.2. |
| 2026-06-26 | 3.3 / 5.3 / 5.4 / Estado | Fase 2 (banco). Aplicadas `0003_triggers` (`trigger_set_timestamp`, `gerar_codigo_chamado` com contador anual atômico, `calcular_sla_chamado` com escada C1, `handle_new_user`), `0004_rls_policies` (helpers `auth_role`/`auth_empresa_id` SECURITY DEFINER, **RLS habilitado nas 7 tabelas + `contador_chamados`**, policies por papel da Seção 3.3, grants com `anon` sem acesso e imutabilidade de `mensagens`/`historico`) e `0005_harden_functions` (search_path fixo + REVOKE de EXECUTE nas functions de trigger). Smoke test em transação revertida confirmou código `BOND-YYYY-00001`, SLA URGENTE = 50% de ALTA e perfil CLIENTE automático. Advisor de segurança: 0 erros (resta apenas INFO de `contador_chamados` deny-all, intencional). |
| 2026-06-30 | Consolidação de branches / 3.2 / 3.3 / 5.1 / 5.3 / 6 / Estado | **Consolidação:** backend (branch `supabase-table-setup`) + protótipos (branch `portal-screen-prototypes`) reunidos na branch de trabalho, com **um único MD autoritativo** para todo o projeto (as outras branches tinham cópias desatualizadas — esta passa a ser a fonte de verdade). **Avaliação (CSAT 1–5):** migration `0006_avaliacao_chamado` (colunas `avaliacao_*` em `chamados` + CHECK 1–5, policy `chamados_update_cliente_avaliacao` restrita a autor + `RESOLVIDO`, trigger `enforce_cliente_so_avaliacao` travando colunas, índice CSAT) aplicada no projeto `iurlzlhbnoemkzgexcfk`; advisors sem novos erros. **Decisão de produto:** abertura de chamado é por **Categoria + Assunto** — campo "Produto relacionado" removido do protótipo (`build.py` + `cliente-novo-chamado.html`). **Fase 3 (início):** Portal do Cliente em produção — rotas `/portal` (dashboard, novo chamado, detalhe, mensagens, avaliação), repositório `asyncpg`+RLS, templates Jinja com tokens da marca e widget de estrelas; CSAT deixa de depender de e-mail (Fase 5). Suíte pytest 30 testes verde (9 anteriores + 21 do portal/avaliação). |
| 2026-07-01 | 3.2 / 3.3 / 6.1 / Estado | **Refino do modelo de acesso + Chat Realtime + Vendoring.** (a) **Acesso (migration `0010_acesso_ti_total`):** o acesso total passa a ser do departamento **TI** (`auth_is_ti()`), não mais "ADMIN com departamento NULO"; RH/Marketing veem seu setor + os que abriram; funcionário vê só os próprios. Gestão de catálogos = TI. **Validado ponta a ponta contra o Supabase live** (simulação de claims por usuário; ver Seção 3.3). (b) **Chat Realtime (Seção 6.1, migration `0011_realtime_mensagens`):** `mensagens` adicionada à publicação `supabase_realtime`; `app/static/js/chat.js` (CSP-safe) assina `postgres_changes` filtrando por `chamado_id` com o JWT do usuário e dispara refresh HTMX; fragmento `GET /portal/chamados/{id}/mensagens/fragmento` + polling 10s de fallback; RLS aplicada (nota interna não vaza). (c) **Vendoring self-host com SRI:** HTMX 2.0.4, Alpine CSP 3.14.8 e supabase-js 2.47.10 em `app/static/vendor/` (via npm — CDNs bloqueados por egress; `registry.npmjs.org` liberado), carregados sob `script-src 'self'`. (d) Portal agora aberto a qualquer autenticado (staff também abre chamados); "meus chamados" filtra por autor. Suíte pytest **48 testes** verde. |
| 2026-07-01 | 3.2 / 3.3 / 5.1 / 5.3 / 3.4 / Prototipos / Estado | **Pivô para SISTEMA INTERNO + roteamento por DEPARTAMENTO.** Migrations `0008_departamentos_roteamento` e `0009_harden_auth_departamento` aplicadas no projeto `iurlzlhbnoemkzgexcfk`. Nova tabela `departamentos` (seed TI/RH/Marketing, gerenciável); `perfis.departamento_id` (setor do staff; NULO = super-admin/funcionário) e `chamados.departamento_id` (destino). **RLS repontada de empresa→departamento/autor:** funcionário vê os próprios chamados; staff vê só o seu departamento; **super-admin** (ADMIN, `departamento_id` NULO) vê tudo; mensagens/histórico seguem o mesmo escopo; helper `auth_departamento_id()`. `empresas`/`planos_sla` viram **config interna de SLA** (org única `Bondmann Química` semeada); `handle_new_user` vincula o novo usuário a ela. **Sem signup público** — cadastro direto no Supabase + `supabase/registro_usuarios.sql` (promoção de papel/departamento). Backend: `chamados.criar` exige `departamento_id`; `departamentos_ativos`; portal com seletor de departamento; detalhe exibe o setor; rotas `/cadastro` removidas. **Protótipos** (`build.py`): removidas telas de Empresas, Planos de SLA e Cadastro; abertura de chamado passa a ter Departamento. Suíte pytest **47 testes** verde. Advisors: só INFO de `contador_chamados` + os 3 `WARN` pré-existentes de helpers SECURITY DEFINER (agora incl. `auth_departamento_id`, mesmo padrão; backlog: schema privado). |
| 2026-07-01 | Organização de branches / 3.9 / 1.4 / Estado | **Organização de branches:** trabalho consolidado passa a viver em **`claude/develop`** (branch principal de desenvolvimento, criada a partir de `ticket-rating-implementation`); **`claude/Md`** mantida como **fonte única do MD** (somente o documento, sempre sincronizada byte-a-byte com o MD de `develop`); mantida `portal-screen-prototypes` como referência; removidas as branches redundantes (`antigravity-protocol`, `project-implementation-continue`, `supabase-table-setup` [já mesclada], `ticket-rating-implementation` [contida em `develop`]). **Fase 3 — Storage de anexos (Seção 3.9):** migration `0007_storage_anexos` (bucket privado `chamados-anexos` + RLS path-scoped `{empresa_id}/{chamado_id}/{arquivo}`, policies SELECT/INSERT por tenant e DELETE só staff) aplicada no projeto `iurlzlhbnoemkzgexcfk`. Backend: `app/security/uploads.py` (validação server-side — limite 10MB, allow-list `pdf/jpg/png/mp4/docx/xlsx`, **MIME real por magic bytes** via python-magic + sniffer de fallback, sanitização de nome → UUID+ext); `app/storage.py` (acesso **direto à REST do Storage via httpx com o JWT do usuário** — respeita RLS, contorna a superfície async incompleta do supabase-py, Seção 1.4; **signed URLs TTL 1h geradas on-demand a cada render**, C2); rota de resposta agora aceita anexos multipart e o detalhe renderiza links assinados. Suíte pytest **45 testes** verde (30 anteriores + validação de upload + rota de anexos). **Nota de segurança (follow-up):** advisor aponta `WARN` em `auth_role()`/`auth_empresa_id()` (SECURITY DEFINER expostas via RPC ao `authenticated`) — **não** podem ter `EXECUTE` revogado sem quebrar a avaliação das policies; hardening correto = mover para schema privado não exposto ao PostgREST (backlog, não bloqueante — retornam só dados do próprio usuário). |
