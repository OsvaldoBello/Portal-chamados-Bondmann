# Dependências de desenvolvimento/testes (Seção 0.3)
-r requirements.txt
pytest==8.3.4
pytest-asyncio==0.24.0
# Cobertura de testes — gate de CI (Sprint 3 / item 3.1). O floor mínimo fica em
# [tool.coverage]/CI ~2 pontos abaixo do valor medido (adoção gradual, não um
# número aspiracional que quebre o build).
pytest-cov==6.0.0
ruff==0.8.6
# Type-checking gradual — gate de CI (Sprint 3 / item 3.1). Cobre só os módulos
# onde correção de tipo importa mais (ver [tool.mypy] no pyproject.toml), não a
# árvore inteira e sem --strict; alargar depois.
mypy==1.14.1
